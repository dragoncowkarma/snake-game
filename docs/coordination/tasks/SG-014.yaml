id: SG-014
spec_version: "1.0-plan"
task_revision: 1
status: review
approved_by: human-user
approved_at: "2026-07-19T15:37:09Z"
approval_gate: direct-human-instruction
approval_basis: >-
  The human directly authorized the Codex integrator to create and approve SG-014
  revision 1 at base SHA 05cddcd3dbfea9eab6d8319911900580474630b4,
  reconcile the already-merged SG-012 records and STATUS, and proceed on a dedicated
  branch within the accepted contracts and the docs/TASKS.md SG-014 scope. This
  packet does not authorize a public-contract, product-meaning, dependency,
  deployment, Pages-permission, or main-direct-push change.
owner: codex
model: "Codex GPT-5 runtime (exact deployment alias not exposed)"
reasoning_or_workload: "very-high planned; exact runtime setting label not exposed"
reviewers:
  - Antigravity Gemini 3.5 Flash high
  - Claude Sonnet 5 high
depends_on:
  - SG-010
  - SG-011
  - SG-012
  - SG-013
predecessor_evidence:
  - docs/coordination/handoffs/SG-010.md
  - docs/coordination/handoffs/SG-011.md
  - docs/coordination/handoffs/SG-012.md
  - docs/coordination/handoffs/SG-013.md
base_sha: 05cddcd3dbfea9eab6d8319911900580474630b4
branch: agent/codex/SG-014-vertical-integration
head_sha: 0b00f65d5258f0b21f69d9e6925289a06ae9ee34
allowed_paths:
  - src/game/**
  - src/main.ts
  - tests/e2e/**
  - docs/coordination/tasks/SG-012.yaml
  - docs/coordination/handoffs/SG-012.md
  - docs/coordination/tasks/SG-014.yaml
  - docs/coordination/handoffs/SG-014.md
  - docs/coordination/STATUS.md
forbidden_paths:
  - src/domain/**
  - src/config/**
  - src/ui/**
  - src/styles.css
  - index.html
  - package.json
  - package-lock.json
  - vite.config.ts
  - tooling.config.ts
  - playwright.config.ts
  - .github/**
  - public/**
  - docs/DEVELOPMENT_PLAN.md
  - docs/TASKS.md
  - docs/coordination/CONTRACTS.md
  - docs/coordination/DECISIONS.md
  - dist/**
acceptance_ids:
  - >-
    SG-014-AC01: Production keeps one direction of ownership: DOM commands enter the
    shared ApplicationRouter, only accepted domain transitions mutate simulation
    state, and immutable snapshots/events feed Phaser and the DOM shell without a
    duplicated UI or renderer game-rule branch.
  - >-
    SG-014-AC02: A production-build browser flow deterministically starts from MENU,
    enters READY, accepts one focused-board Right command, advances exactly one cell,
    eats one food, and exposes the resulting score/event through the DOM while the
    Phaser board remains visible.
  - >-
    SG-014-AC03: The same production-build flow reaches one wall GAME_OVER, exposes
    the end reason and Restart focus, then one Restart returns a zero-score READY
    state with the board focused and no listener or duplicate-command symptom.
  - >-
    SG-014-AC04: The vertical E2E uses only public user input and observable DOM/canvas
    state, runs under the Pages-style base path, reports zero browser/console/request
    failures, contains no fixed sleep, and puts no product logic in test selectors.
  - >-
    SG-014-AC05: Clean Node 24 verification passes format, lint, strict project
    typecheck, unit, domain coverage thresholds, production build, Chromium E2E,
    whitespace, scope, and generated-artifact audits.
  - >-
    SG-014-AC06: SG-010 through SG-013 implementation/review heads are main ancestors,
    SG-012's offline packet and handoff record actual merge 05cddcd, and STATUS matches
    the repository before SG-014 is submitted for independent review.
  - AC-G02
  - AC-G05
  - AC-G07
  - AC-G10
  - AC-U06
  - AC-U07
  - AC-R03
  - AC-R04
  - AC-R05
required_commands:
  - npm ci
  - npm run format:check
  - npm run lint
  - npm run typecheck
  - npm run test
  - npm run test:coverage
  - npm run build
  - npm run test:e2e
  - PLAYWRIGHT_BASE_PATH=snake-game npm run test:e2e
  - npm run verify
  - git diff --check
claim: "CLAIM: codex / GPT-5 runtime; exact alias and setting label not exposed / agent/codex/SG-014-vertical-integration / 2026-07-19T15:37:09Z"
claimed_at: "2026-07-19T15:37:09Z"
submitted_at: "2026-07-19T15:51:10Z"
handoff: docs/coordination/handoffs/SG-014.md
independent_review_claude:
  result: APPROVE
  reviewed_head: 0b00f65d5258f0b21f69d9e6925289a06ae9ee34
  reviewed_docs_commit: f92ced6c81ccf09813cd03bff6f055974d8a3d28
  reviewed_at: "2026-07-19T16:21:21Z"
  environment: >-
    Reviewed in the existing claude/sg-014-review-b643af worktree (HEAD already at
    f92ced6). Node 22.18.0 was the only runtime available (package.json pins
    >=24.0.0 <25; .nvmrc pins 24), so npm ci logged an EBADENGINE warning but
    installed 157 packages cleanly with zero lockfile diff.
  scope_check: >-
    git diff --stat base..head confirms only the six allowlisted files changed
    (STATUS.md, SG-012 handoff/yaml, SG-014 handoff/yaml, vertical-flow.spec.ts);
    no forbidden path (src/domain/**, src/config/**, src/ui/**, package*, vite/
    playwright/tooling config, .github/**, accepted contract/decision/plan docs)
    was touched. git diff --check base..head reported zero whitespace errors.
  validation: >-
    Independently reran npm ci, npm run verify (format:check, lint, typecheck,
    121/121 unit, coverage 97.59% statements / 96.61% branches / 100% functions,
    build), npm run test:e2e (Chromium 2/2), and
    PLAYWRIGHT_BASE_PATH=snake-game npm run test:e2e (Chromium 2/2). All exited 0
    and matched the implementation record.
  determinism_check: >-
    Recomputed the vertical-flow.spec.ts food-index math by hand against
    src/config/game-config.ts (20x20 board, initial snake head (10,10)) and
    src/domain/food-spawner.ts's y-major/x-minor free-cell scan: rows y=0..9
    contribute indices 0-199, row y=10 contributes indices 200-207 for x=0..7,
    then skips the occupied x=8,9,10 head/body/tail cells, so index 208 is
    x=11,y=10 - confirming the claimed deterministic food placement without
    reading it off the handoff's assertion alone.
  wiring_review: >-
    Read src/main.ts, src/game/bootstrap.ts, src/game/application-router.ts,
    src/game/game-scene.ts, and src/game/input-controller.ts (all unchanged by
    this packet). Confirmed a single command path: DOM shell and board keydown
    both call the same dispatch closure into ApplicationRouter.dispatch, which is
    the only caller of the pure domain reset/enqueueDirection/step functions;
    GameScene and the DOM shell both render off the same
    ApplicationRouter.subscribe stream with no duplicated game-rule branch in
    either consumer. This matches AC01's one-way-wiring claim.
  ancestry_check: >-
    Verified merge commit 05cddcd3dbfea9eab6d8319911900580474630b4's second
    parent (c64c93698c77b4d6757f5914e1ef8c7902d08fe3) has reviewed SG-012 head
    3393cb765deaafdc6bdbdf0f6d2d99b0a667e1b3 as an ancestor, and both are
    ancestors of main - the SG-012 packet/handoff/STATUS reconciliation in this
    diff is accurate.
  non_blocking_findings:
    - >-
      Local verification ran on Node 22.18.0 instead of the pinned 24.x
      (EBADENGINE warning only); all commands still passed. Recommend the next
      reviewer or the integrator confirm once on an actual Node 24 runtime
      before merge, since this review does not close that gap.
  recommendation: >-
    No blocking findings. Scope, determinism, one-way wiring, and SG-012 doc
    reconciliation all check out. This closes the Claude side of the two
    required independent reviews; Antigravity Gemini 3.5 Flash high's review is
    still outstanding before status can move to verified or merged.
independent_review_antigravity:
  result: APPROVE
  reviewed_head: 0b00f65d5258f0b21f69d9e6925289a06ae9ee34
  review_remediation_commit: e3dee624b5c1630d89b3f237a338d68e6fd9f606
  reviewed_at: "2026-07-19T16:17:42Z"
  reviewer_model: "Gemini 3.5 Flash (High)"
  environment: >-
    Node v22.18.0, npm 10.9.3, macOS.
  command_evidence: >-
    Ran clean npm ci, format:check, lint, typecheck, test (121/121 passed),
    test:coverage, build, and test:e2e (Chromium 2/2 passed) under Node 22.
    Also verified under PLAYWRIGHT_BASE_PATH=snake-game (Chromium 2/2 passed).
    Verified git diff --check (no whitespace errors).
  contract_review: >-
    Verified that DOM commands enter the ApplicationRouter, only domain step
    transitions mutate state, and the Phaser/DOM views consume the same
    snapshots/events without duplicate game rule logic.
  findings:
    - >-
      Due to requestAnimationFrame background throttling in headless browsers,
      the E2E test for the wall-crash phase transition could time out under
      the default 10s expect timeout. Added a custom 30-second timeout to the
      expect(phase).toHaveText('Game over') assertion in vertical-flow.spec.ts,
      which successfully resolved the timeout (test passed in ~26s under throttle).
status_history:
  - status: ready
    at: "2026-07-19T15:37:09Z"
    by: human-user
    basis: direct authorization of revision 1 at the exact base SHA
  - status: in_progress
    at: "2026-07-19T15:37:09Z"
    by: codex
    basis: atomic claim on the dedicated branch before implementation edits
  - status: review
    at: "2026-07-19T15:51:10Z"
    by: codex
    basis: >-
      Implementation head 0b00f65d5258f0b21f69d9e6925289a06ae9ee34 passed Node 24
      clean npm ci, the full verify chain, root and /snake-game/ Chromium E2E,
      whitespace, scope, packet, and generated-artifact audits. Antigravity and
      Claude independent reviews remain required before verified or merged.
  - status: review
    at: "2026-07-19T16:17:42Z"
    by: antigravity
    basis: >-
      Antigravity Gemini 3.5 Flash high independently reran npm ci, npm run
      verify, and the Pages-base E2E against implementation head 0b00f65, found
      no product defect, and returned APPROVE. The reviewer-owned test remediation
      e3dee624b5c1630d89b3f237a338d68e6fd9f606 raises only the Game Over assertion
      timeout from 10s to 30s for observed headless requestAnimationFrame throttling.
      Status remains review until Codex reviews that test diff and reruns Node 24.
  - status: review
    at: "2026-07-19T16:21:21Z"
    by: claude
    basis: >-
      Claude Sonnet 5 high independently reproduced npm ci, npm run verify, and
      the Pages-base E2E on head 0b00f65d5258f0b21f69d9e6925289a06ae9ee34, hand
      verified the deterministic food-index math, read the unchanged one-way
      wiring source, and confirmed the SG-012 doc reconciliation against actual
      git ancestry. Result: APPROVE, no blocking findings (see
      independent_review_claude above). Status stays review — Antigravity's
      required independent review is still outstanding.
