id: SG-017
spec_version: "1.0-plan"
task_revision: 1
status: merged
approved_by: human-user
approved_at: "2026-07-20T09:42:13Z"
approval_gate: direct-human-instruction
approval_basis: >-
  STATUS.md recorded SG-017 as "packet 승인 대기" (packet awaiting approval) with no
  docs/coordination/tasks/SG-017.yaml and no GitHub Issue/PR (0 open, confirmed via
  `gh issue list`/`gh pr list`). AGENTS.md section 3 requires approval fields
  (revision/approved_by/approved_at/status: ready) before starting, and this session's
  initial prompt template arrived with those fields as unfilled placeholders. Per
  AGENTS.md ("승인 필드가 없거나... 시작하지 마라") the agent stopped before touching any
  code and asked the human directly via AskUserQuestion whether to author and execute
  this exact revision-1 offline packet under the TASKS.md SG-017 row scope (allowed
  paths src/ui/**, styles, copy docs; depends_on SG-013, SG-016; AC-U06~U09). The human
  selected "직접 승인하고 진행" (direct authorization to proceed), the same
  approval_gate pattern already used for SG-016. This authorizes only the SG-017 scope
  already defined in the accepted docs/TASKS.md row; it does not authorize a
  public-contract or product-meaning change, a new dependency, deployment, or
  main-direct-push.
owner: claude
model: "Claude Sonnet 5 high"
reasoning_or_workload: high
reviewers:
  - Antigravity Gemini 3.1 Pro high
depends_on:
  - SG-013
  - SG-016
predecessor_evidence:
  - docs/coordination/handoffs/SG-013.md
  - docs/coordination/handoffs/SG-016.md
base_sha: cb7869958dd5782d2a05df165e2fe7eaeabbfc78
branch: claude/sg-017-ui-implementation-2aeace
implementation_head_sha: 0d763bfb165513bc0287e2ed6a35ef23863658aa
head_sha: ea82d2e14055f3867f5ba63feacf9d0d4d23943d
integration_base: cb7869958dd5782d2a05df165e2fe7eaeabbfc78
integration_commit: 526fe5cebf4a7af06d67f7bf75558a37411cc452
integrated_at: "2026-07-20T10:58:02Z"
allowed_paths:
  - src/ui/**
  - src/styles.css
  - src/game/bootstrap.ts
  - tests/e2e/qa-audit.spec.ts
  - docs/coordination/tasks/SG-017.yaml
  - docs/coordination/handoffs/SG-017.md
  - docs/coordination/STATUS.md
allowed_paths_note: >-
  src/game/bootstrap.ts and tests/e2e/qa-audit.spec.ts sit outside SG-017's default
  ownership table row (src/ui/**, styles, copy docs) but are included as narrow,
  justified one-time exceptions, following the same precedent SG-013 recorded for
  STATUS.md/TASKS.md: (1) src/ui/**'s new GameShellHandle.updateMeta/ShellMeta surface
  is inert unless the single production integrator (mountPhaserGame in bootstrap.ts)
  actually calls it after toggleMute and after each snapshot; without touching that one
  file the DF-SG015-01 fix and persisted-best binding cannot reach production, and the
  edit is limited to wiring calls into the existing dispatch/subscribe closures, not new
  game rules, Phaser Scene code, or adapter logic. (2) tests/e2e/qa-audit.spec.ts's one
  `test.fail(true, ...)` marker on the DF-SG015-01 case exists specifically to let
  `npm run test:e2e`/`npm run verify` pass while that defect is open; fixing the defect
  without removing that marker would flip the suite red (Playwright reports an
  expected-to-fail test that passes as a failure), so leaving the file untouched is not
  an option once the underlying bug is fixed. No other line in either file is changed.
forbidden_paths:
  - src/domain/**
  - src/config/**
  - src/game/application-router.ts
  - src/game/game-scene.ts
  - src/game/board-renderer.ts
  - src/game/fixed-step-scheduler.ts
  - src/game/input-controller.ts
  - src/game/lifecycle-controller.ts
  - src/adapters/**
  - tests/unit/**
  - tests/fixtures/**
  - tests/helpers/**
  - tests/e2e/vertical-flow.spec.ts
  - tests/e2e/scaffold.spec.ts
  - package.json
  - package-lock.json
  - vite.config.ts
  - playwright.config.ts
  - tooling.config.ts
  - .github/**
  - public/**
  - docs/DEVELOPMENT_PLAN.md
  - docs/TASKS.md
  - docs/coordination/CONTRACTS.md
  - docs/coordination/DECISIONS.md
  - dist/**
acceptance_ids:
  - >-
    SG-017-AC01 / AC-U06: focus restoration (board after start/resume-shaped entry,
    Resume after pause, Restart after gameOver/won, Start after menu) remains correct
    after the ShellMeta/updateMeta refactor of createGameShell; no regression in
    focusTargetForTransition usage.
  - >-
    SG-017-AC02 / AC-U07, DF-SG015-01: score/best/phase/end-reason stay in the DOM and
    at most one aria-live announcement fires per meaningful change; the keyboard 'm'
    mute shortcut and the Mute button both drive the exact same visible mute state
    (aria-pressed, textContent) via the audio adapter's authoritative isMuted value,
    closing DF-SG015-01.
  - >-
    SG-017-AC03 / "최고 기록" UI, DEVELOPMENT_PLAN section 3.5: the persisted
    per-difficulty high score (GamePreferences, SG-016) is bound to the HUD Best line
    on every snapshot and on difficulty selection, and a new best achieved during the
    run that just ended is textually highlighted only on the terminal (gameOver/won)
    screen, never mid-play.
  - >-
    SG-017-AC04 / AC-U08, AC-U09: no new colors, layout, or touch-target sizing were
    introduced; the new best-score emphasis reuses already-validated tokens (bold
    weight + existing --sg-text foreground, no new hue) so no new contrast validation
    is required, and no change touches touch-action/safe-area/44px rules.
  - AC-U06
  - AC-U07
  - AC-U08
  - AC-U09
required_commands:
  - npm run format:check
  - npm run lint
  - npm run typecheck
  - npm run test
  - npm run build
  - npm run test:e2e
  - npm run verify
  - git diff --check
claim: "CLAIM: claude / Claude Sonnet 5 high / claude/sg-017-ui-implementation-2aeace / 2026-07-20T09:42:13Z"
claimed_at: "2026-07-20T09:42:13Z"
handoff: docs/coordination/handoffs/SG-017.md
independent_review_antigravity:
  result: APPROVE
  reviewed_head: 0d763bfb165513bc0287e2ed6a35ef23863658aa
  reviewed_at: "2026-07-20T10:47:17Z"
  evidence_form_caveat: >-
    Received as a chat-relayed transcript in this session, not as a repository-native
    artifact (no separate reviewer branch, commit, or PR/Issue comment exists for it).
    AGENTS.md section 2 treats inter-AI chat messages as notifications only, not
    completion evidence on their own; this entry preserves what was reported so a
    later integrator can decide whether an additional independently-reproducible
    verification pass is warranted before merge.
  reported_findings: >-
    DF-SG015-01/AC-U07 mute sync, AC-U03/U08/U09 persisted best-score binding and
    terminal-only new-best display, and CSS token reuse were each checked against the
    handoff and judged correct with no remediation required. The reviewer additionally
    reran `npm ci && npm run verify` on Node 24 independently in the background and
    reported the same result this session already recorded: format, lint, typecheck,
    128 unit tests, coverage, build, and Chromium E2E all exit 0.
status_history:
  - status: ready
    at: "2026-07-20T09:42:13Z"
    by: human-user
    basis: direct authorization via AskUserQuestion to create and execute this exact revision-1 offline packet
  - status: in_progress
    at: "2026-07-20T09:42:13Z"
    by: claude
    basis: claimed on the existing dedicated branch before any implementation edits
  - status: review
    at: "2026-07-20T10:26:25Z"
    by: claude
    basis: >-
      Implementation head 0d763bfb165513bc0287e2ed6a35ef23863658aa passed Node 24
      format, lint, typecheck, 128 unit tests, coverage (97.59% stmts / 96.61%
      branches, unchanged from SG-011 baseline), build, and Chromium production E2E
      7/7 (including the previously test.fail-marked DF-SG015-01 case, now genuinely
      passing). Manual Claude Browser reproduction against the real
      mountPhaserGame/GamePreferences wiring confirmed persisted best-score binding
      across a simulated reload and terminal-only new-best display. Required
      independent Antigravity review has not run yet; no local main merge performed.
  - status: verified
    at: "2026-07-20T10:47:17Z"
    by: claude
    basis: >-
      Independent Antigravity Gemini 3.1 Pro high review (see
      independent_review_antigravity above) returned APPROVE on head
      0d763bfb165513bc0287e2ed6a35ef23863658aa with no remediation required. Per
      AGENTS.md section 4, merge into local `main` is the coordination lead's
      (default Codex) step, not this Claude UI-implementer session's; status is
      advanced to `verified` but not `merged` here. No local main merge, remote push,
      or deployment was performed by this session.
  - status: merged
    at: "2026-07-20T10:58:02Z"
    by: claude
    basis: >-
      The human directly instructed "머지" (merge) after reviewing the recorded
      Antigravity APPROVE, overriding the role-separation default this session had
      flagged (normally the coordination lead/Codex merges). `main` was at the exact
      base_sha (cb7869958dd5782d2a05df165e2fe7eaeabbfc78) with zero divergence since
      this branch was cut, so `git merge --no-ff claude/sg-017-ui-implementation-2aeace`
      from the primary worktree (/Volumes/NO NAME/workspace/snake-game, where `main`
      is checked out — this worktree cannot check out a branch checked out elsewhere)
      completed with the 'ort' strategy and zero conflicts as integration_commit
      526fe5cebf4a7af06d67f7bf75558a37411cc452. `git diff --quiet` between the merge
      commit and branch tip ea82d2e14055f3867f5ba63feacf9d0d4d23943d confirmed byte-
      identical trees. Re-running `npm ci`/`npm run verify` in that primary worktree
      was blocked by this session's sandbox classifier (outside the declared worktree
      root); since the merge introduced no new tree content beyond what this session
      already verified clean on the branch tip (format, lint, typecheck, 128 unit
      tests, coverage, build, Chromium E2E 7/7, all on Node 24), no separate post-
      merge verification run exists as an independent artifact — this is recorded
      transparently rather than assumed. No remote push or deployment was performed.
      CORRECTION: the sandbox classifier block on npm ci/verify in the primary
      worktree was transient, not permanent (see the next status_history entry) — a
      real post-merge verification run in the primary worktree was completed
      afterward and superseded this entry's "no separate post-merge run" statement.
  - status: merged
    at: "2026-07-20T11:48:57Z"
    by: claude
    basis: >-
      CORRECTS THE PRECEDING ENTRY. The sandbox classifier allowed npm ci/npm run
      verify in the primary worktree on retry. The background run was piped through
      `| tail -100`, and the task-notification system reported that pipeline's exit
      code (tail's, always 0) as "completed (exit code 0)" — this was wrongly relayed
      to the human as a clean verify pass. Re-running `npm run test` alone (no pipe)
      showed the real exit code was 1: 19 test files failed to parse, all of them
      pre-existing `spikes/phaser-4.2.1/e2e/spike.spec.cts` Playwright spec files
      living inside OTHER, unrelated `.claude/worktrees/*` checkouts
      (sg-013-ui-implementation-bd0257, sg-014-review-b643af, sg-016-review-e54951,
      wave-2-completion-793b29) that happen to be nested inside the primary
      worktree's own directory tree. package.json's `test` script excludes
      `spikes/**` (anchored at repo root only, not `**/spikes/**`), so it never
      excluded these nested copies; 796/796 real test assertions that did run all
      passed, and zero of the 19 failing files touch anything SG-017 changed. This is
      a pre-existing, unrelated tooling defect (flagged separately, out of this
      packet's scope, not fixed here since it requires editing vite.config.ts/
      package.json, both forbidden paths for this packet). To get real evidence for
      the merged tree, each required command was re-run individually with true
      (unmasked) exit codes from the primary worktree: `npm run format:check` (0),
      `npm run lint` (0), `npm run typecheck` (0), a properly-scoped
      `npx vitest run --exclude "tests/e2e/**" --exclude "spikes/**" --exclude
      ".claude/worktrees/**" --exclude ".codex/worktrees/**" --exclude ".gemini/**"`
      (0; 12 files / 128 tests passed, matching the branch-tip result exactly),
      `npm run build` (0), and `npm run test:e2e` (0; 7/7 Chromium PASS, unaffected
      since Playwright's testDir is explicitly scoped to ./tests/e2e). All on Node
      24.18.0. The literal `npm run test`/`npm run verify` npm scripts will still
      fail with exit 1 if invoked from the primary worktree until the separately-
      flagged glob-anchoring defect is fixed; this is expected and not a SG-017
      regression.
