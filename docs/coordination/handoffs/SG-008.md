# 작업 인계: `SG-008`

## 식별 정보

- 담당 AI / 모델 / 설정: Antigravity / gemini-3.5-flash-high / default
- 명세 버전 / task packet revision: `1.0-plan` / revision 1
- 기준 SHA(`base_sha`): `0fae00a65d662592b28f5daf59edcfc21584ac65`
- 브랜치 / 결과 SHA(`head_sha`): `test-engineering-sg-008` / `c857f2bc08bf84ab4e12e7222664e46cb8686d4f`
- 연결 Issue / PR: `docs/coordination/tasks/SG-008.yaml` offline 원본
- 승인자 / 승인 시각: human-user / `2026-07-19` (direct chat instruction)
- 상태: `merged` — local integration commit `522fcb7ebb3460b5dce858cc23bae825866e9681`; 원격 push·배포 없음

## 수행 내용

- 변경 목적:
  - AC 기반 24개 테스트 피스처 및 실패 보고서(EV-FAIL-01) 기반 구축
  - 결정론적 seed helper(SeededRandomSource Mulberry32) 및 ScriptedRandomSource 구현
  - Playwright E2E 테스트 공용 리스너(setupPageListeners) 추출 및 scaffold.spec.ts 리팩토링
- 변경 파일:
  - `docs/coordination/tasks/SG-008.yaml` (task packet)
  - `tests/fixtures/fixtures.ts` (24 fixtures definition)
  - `tests/fixtures.test.ts` (invariants validation and serialization suite)
  - `tests/helpers/rng.ts` (random source helpers)
  - `tests/helpers/ev-fail-01.ts` (EV-FAIL-01 types)
  - `tests/helpers/e2e-helpers.ts` (E2E page listeners helper)
  - `tests/e2e/scaffold.spec.ts` (refactored to use the E2E helper)
- 의도적으로 변경하지 않은 범위:
  - `src/**` (제품 도메인/UI 코드)
  - `playwright.config.ts` (설정 감사 후 무수정 유지)
  - `.github/workflows/quality.yml` (CI workflow)

## 수용 기준 증거

| AC | 증거(테스트 이름, 스크린샷, 로그 또는 수동 절차) | 결과 |
|---|---|---|
| `SG-008-AC01` / `QA_PLAN.md` L331 | 24개의 피스처와 `EV-FAIL-01` 스키마가 json 파일로 성공적으로 직렬화되어 workspace-local `test-results/fixtures/` 아래에 `SG-008_<ID>_SCHEMA_<git-HEAD-SHA12>_node24_<run-UTC-date>.json` 패턴으로 저장됨 | PASS |
| `SG-008-AC02` | `tests/fixtures.test.ts` 에서 `SeededRandomSource`와 `ScriptedRandomSource` 동작 검증 완료 | PASS |
| `SG-008-AC03` | 모든 GameState 피스처가 Manhattan contiguity, unique snake, score-foodsEaten ratio, tickMs formula 등의 invariants 검증 통과 | PASS |
| `SG-008-AC04` | `npm run verify`를 실행하여 포맷, 린트, 타입스크립트 타입 체크, unit 테스트, preview E2E 테스트 전체 통과 | PASS |

## 실행한 검증

| 명령/환경 | exit code | 실제 결과 |
|---|---:|---|
| `npm run format:check` | 0 | 포맷 통과 |
| `npm run lint` | 0 | ESLint 경고/오류 0건 통과 |
| `npm run typecheck` | 0 | TypeScript strict compile 통과 |
| `npm run test` | 0 | Vitest 3개 파일 57개 테스트 PASS (`tests/fixtures.test.ts` 포함) |
| `npm run build` | 0 | Vite build 성공 |
| `npm run test:e2e` | 0 | Playwright E2E 1개 테스트 PASS (scaffold.spec.ts) |
| `git diff --check` | 0 | whitespace 오류 0건 통과 |

추가 Codex 통합 재검증:
- 최종 SG-008 구현 head `c857f2bc08bf84ab4e12e7222664e46cb8686d4f`에서 `npm run format:check`, `npm run lint`, `npm run typecheck`, `npm run test`, `npm run build`, `git diff --check`가 모두 exit 0이었다.
- 최신 `main`(SG-007 포함)에 SG-008을 병합한 commit `522fcb7ebb3460b5dce858cc23bae825866e9681`에서도 `npm run format:check`, `npm run lint`, `npm run typecheck`, `npm run test`(4 files / 68 tests), `npm run build`, `git diff --check`가 모두 exit 0이었다.
- `npm run test` 직후 `test-results/fixtures/` 아래 25개 schema 파일이 생성되고 filename SHA12가 현재 checkout SHA12와 일치함을 확인했다. SG-008 구현 head에서는 `c857f2bc08bf`, completion merge에서는 `522fcb7ebb34`다.
- sandbox 내부 `npm run test:e2e` 첫 시도는 localhost bind `EPERM`으로 실패했지만, 같은 명령을 local-server 권한 환경에서 재실행해 Chromium 1/1 PASS했다. 이 확인도 SG-008 구현 head와 completion merge 양쪽에서 수행했다.
- 이후 verified 결과를 최신 local `main`(SG-007 포함)에 merge commit `522fcb7ebb3460b5dce858cc23bae825866e9681`으로 충돌 없이 통합했다.

## 결정과 위험

- 새 의존성: 없음
- 새 결정 또는 기존 결정과의 차이: 없음. 모든 타입과 속성은 `CONTRACTS.md` 및 `QA_PLAN.md`를 100% 준수함.
- 알려진 위험/제약:
  - `test-results/fixtures/`는 Playwright `outputDir` 아래라 `npm run test:e2e` 또는 `npm run verify` 뒤 삭제된다. schema 파일 확인은 아래 재현 절차처럼 `npm run test` 직후 수행한다.
- 남은 작업과 권장 담당자:
  - SG-011/SG-012/SG-015 이후 검증 작업은 이 fixture/evidence foundation을 사용하되, 각 task packet의 최신 base SHA와 수용 기준을 새로 기록해야 한다.

## 리뷰어 재현 절차

1. `git switch test-engineering-sg-008`
2. `npm ci`
3. `npm run test` (이 단계에서 `test-results/fixtures/` 디렉토리가 생성되고 25개의 `SG-008_*_SCHEMA_*.json` 파일이 작성됩니다.)
4. `test-results/fixtures/` 디렉토리 아래의 25개 파일 확인 (이후 `npm run test:e2e`나 `npm run verify` 실행 시 Playwright가 `test-results/` 디렉토리를 통째로 자동 삭제하므로, E2E 검증 전에 먼저 확인하는 것이 좋습니다.)
5. `npm run verify`를 실행하여 전체 E2E, 린트, 타입 빌드가 통과하는지 확인.
