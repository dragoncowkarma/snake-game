# 작업 인계: `SG-025`

## 식별 정보

- 담당 AI / 모델 / 설정: Claude / Claude Sonnet 4.6 / 중간
- 명세 버전 / task packet revision: `1.0-plan` / revision 1
- 기준 SHA(`base_sha`): `05153ff967f949fb0c7fb53ddb8490f108629068`
- 브랜치 / 결과 SHA(`head_sha`): `claude/product-docs-responsibility-e22342` / `55d2202731fe7b58c25c0e4c19779416ff7d2205`
- 연결 Issue / PR: `docs/coordination/tasks/SG-025.yaml` offline 원본; GitHub Issue/PR 없음
- 승인자 / 승인 시각: human-user / `2026-07-25T00:00:00Z` (SG-025 프롬프트 수신으로 묵시적 승인)
- 상태: `review-ready` — Codex 독립 리뷰 대기

## 수행 내용

- 변경 목적: 처음 접하는 사람이 설치부터 배포 확인까지 순서대로 재현할 수 있도록 README를 전면 재작성했다. Wave 0 시점에 고착된 구버전 설명을 실제 구현 상태로 교체하고, 게임 조작·난이도·로컬 최고 점수·음소거·접근성 범위·비제공 기능·라이선스를 명확히 기술했다.
- 변경 파일:
  - `README.md`: 게임 설명, 조작법 표, 두 난이도 수치 표, 로컬 최고 점수, 음소거, 접근성 제공 범위와 한계, 비제공 기능 목록, 설치(`npm ci`), 로컬 개발(`npm run dev`), 전체 검증(`npm run verify`), Pages 경로 사전 검증 명령, GitHub Pages 설정, 수동 배포 절차, 배포 확인 항목, 제한된 롤백 4단계, MIT 라이선스, 런타임/개발 의존성 고지, 개발 문서 링크로 전면 교체했다.
  - `docs/coordination/tasks/SG-025.yaml`: SG-025 offline task packet을 신규 작성했다.
- 의도적으로 변경하지 않은 범위: `src/**`, `tests/**`, `.github/**`, `package.json`, `package-lock.json`, `vite.config.ts`, `tooling.config.ts`, `dist`, 원격 push·Pages 배포.

## 수용 기준 증거

| AC | 증거 | 결과 |
|---|---|---|
| `SG-025-AC01` | `npm ci`와 `npm run verify`가 Node 24.18.0에서 exit 0으로 완료됐다. README의 명령과 실제 `package.json` scripts가 일치한다. | PASS |
| `SG-025-AC02` | `npm run build -- --base /snake-game/`와 `PLAYWRIGHT_BASE_PATH=/snake-game/ PLAYWRIGHT_USE_EXISTING_BUILD=1 npm run test:e2e`가 README에 정확히 기술됐다. SG-024 handoff에서 동일 명령이 exit 0과 18/18 PASS를 반환했음을 확인했다. | PASS (기록 대조) |
| `SG-025-AC03` | GitHub Settings → Pages → GitHub Actions 설정, `H3A_APPROVED_RELEASE_SHA` variable 준비, exact-SHA dispatch, `release.json` 확인, 4단계 bounded rollback이 README에 기술됐다. SG-024 workflow 검증 증거와 일치한다. | PASS |
| `SG-025-AC04` | 조작 표에 방향키/WASD, 일시정지(P/Escape), 재시작(Enter/Space), 메뉴, 음소거(M)가 `docs/coordination/CONTRACTS.md`와 `DEVELOPMENT_PLAN.md` 3.4절 계약 그대로 기술됐다. READY Left 거부도 명시했다. | PASS |
| `SG-025-AC05` | 두 난이도 수치 표(시작 속도·가속·최고 속도)가 `src/config/game-config.ts`의 실제 값과 일치한다. localStorage 최고 점수 분리와 음소거 persistence를 기술했다. | PASS |
| `SG-025-AC06` | 접근성 제공 범위(키보드 전체 흐름, 포커스 전이, DOM 텍스트, aria-live, 44px 버튼, reduced-motion, 색상 외 구분, 명암비 목표)와 한계(완전한 비시각적 동등 플레이 미보장, Canvas 격자 미낭독)를 분리해 기술했다. | PASS |
| `SG-025-AC07` | "제공하지 않는 범위" 절에 완전한 비시각적 플레이, 온라인 리더보드/서버/로그인, PWA/Service Worker/완전 오프라인 캐시, 스와이프/배경음악/진동/멀티플레이가 없음을 명시했다. | PASS |
| `SG-025-AC08` | 라이선스 절에 MIT License (c) 2026 dragoncowkarma, Phaser 4.2.1 MIT, 개발 의존성 버전 목록이 포함됐다. `LICENSE` 파일 및 `package.json` 버전과 일치한다. | PASS |
| `SG-025-AC09` | 처음 보는 AI가 README만 읽고 `npm ci` → `npm run verify` → Pages base 검증 → 배포까지 재현할 수 있도록 모든 명령이 인라인 코드블록으로 기술됐다. Codex 독립 리뷰에서 재현 가능성을 확인해달라. | pending review |

## 실행한 검증

| 명령 / 환경 | exit code | 실제 결과 |
|---|---:|---|
| Node 24.18.0 `npm run format:check` | 0 | All matched files use Prettier code style. |
| Node 24.18.0 `npm run verify` (format:check → lint → typecheck → test → test:coverage → build → test:e2e) | 0 | 18/18 Chromium E2E PASS. 기존 Vite chunk 크기 경고와 NO_COLOR 경고는 exit code에 영향 없음. |
| `git diff --check` | 0 | whitespace 오류 0건. |
| `git diff --stat` | — | `README.md` 1파일 변경, `docs/coordination/tasks/SG-025.yaml` 신규 생성. 허용 경로 외 변경 없음. |

실행하지 못한 검증과 이유:

- `npm run build -- --base /snake-game/`와 Pages-base E2E는 SG-024 handoff에서 동일 commit 기반으로 exit 0/18 PASS를 기록했으므로 중복 실행하지 않았다. Codex 리뷰어가 재확인해도 무방하다.
- 실기기 수동 접근성 검증(VoiceOver, TalkBack)은 이 세션 범위 밖이다.

## 결정과 위험

- 새 의존성: 없음.
- 새 결정 또는 기존 결정과의 차이: 없음. DEVELOPMENT_PLAN·CONTRACTS·DECISIONS의 기존 계약을 문서로 노출했을 뿐이다.
- 알려진 위험/제약:
  - DF-SG015-01 (medium): 키보드 `m` 음소거 토글이 aria-pressed와 버튼 텍스트를 갱신하지 않는 기존 결함. README의 음소거 설명은 기능 동작 기준으로 기술했으며 이 결함은 별도 추적 중이다.
  - SG-023 BLOCKED 상태 유지: Firefox rapid-keypress 테스트 결함(제품 결함 아님)과 WebKit macOS 12 툴체인 제약이 해소되지 않았다. README는 검증된 Chromium 명령만 기술했다.
- 남은 작업과 권장 담당자: Codex가 독립 리뷰로 명령 정확성, 배포 절차 일치, 재현 가능성을 확인한다. 이후 SG-028 Claude PASS가 나오면 local main 병합을 조정 책임자가 수행한다.

## 리뷰어 재현 절차

1. `git switch claude/product-docs-responsibility-e22342`하고 head SHA `55d2202731fe7b58c25c0e4c19779416ff7d2205`를 확인한다.
2. Node 24에서 `npm ci`와 `npm run verify`를 실행해 exit 0과 18/18 E2E를 확인한다.
3. README의 모든 명령이 `package.json` scripts와 일치하는지, 난이도 수치가 `src/config/game-config.ts`와 일치하는지, 라이선스 정보가 `LICENSE`·`package.json`과 일치하는지 대조한다.
4. 배포 절차(H3A_APPROVED_RELEASE_SHA variable, exact-SHA dispatch, `release.json` 확인, bounded rollback 4단계)가 `.github/workflows/pages-release.yml` 및 SG-024 handoff와 일치하는지 확인한다.
5. "제공하지 않는 범위" 항목이 `DEVELOPMENT_PLAN.md` 2.4절 MVP 제외 범위와 모순되지 않는지 확인한다.
