# 작업 인계: `SG-022` revision 2 — L1+L3 UI 소유 결함 최소 수정

## 식별 정보

- 담당 AI / 모델 / 설정: Claude / Sonnet 5 / 높음
- 명세 버전 / task packet revision: `1.0-plan` / 2 (M1은 revision 1, 이미 `merged`)
- 기준 SHA(`base_sha`): `f200d7188f8d1d346de9e78be267181ec5a79723` (M1 통합 이후 `main`)
- 브랜치 / 구현 결과 SHA: `agent/claude/SG-022-ui-defects` /
  `7af18a07062902511d755ffedb5865af6c45c9d9`
  (리뷰 반영 후 최종 SHA: `3d0f7a6db446917c2fa3c53edc4037aa83bbaff6`)
- 연결 Issue / PR: 없음 — 승인된 offline packet(`docs/coordination/tasks/SG-022-2.yaml`)
- 승인자 / 승인 시각: human-user / 2026-07-24 (채팅 turn, `docs/coordination/tasks/SG-022.yaml`
  1건, `SG-021` 리포트 1건이 이미 존재해 재작업이 아님을 확인한 뒤 직접 승인)
- 상태: `verified` — 제3 플랫폼 리뷰의 P2 지적을 반영하고 재검증 완료(아래 "독립 리뷰" 절
  참조). **`main` 통합은 수행하지 않음** — 조정 책임자 몫.

## 배경: 브랜치 분기 정정

이 워크트리는 원래 `d490ea3`(SG-019 완료) 기준 `claude/sg-022-minimal-defect-fix-052c0b`
브랜치였다. 그 사이 `main`은 `SG-021`(Claude 리뷰 리포트) → `SG-022` revision 1(M1, Codex의
`Phaser.WEBGL`→`AUTO`)까지 진행됐지만, 이 브랜치는 별도로 `SG-020` negative-control 작업만
쌓은 채 `main`과 공통 조상(`d490ea3`) 이후 완전히 갈라져 있었다. `SG-020` 병합은 조정
책임자(Codex) 소관이라 임의로 합치지 않고, 최신 `main`(`f200d71`) 위에 새 브랜치
`agent/claude/SG-022-ui-defects`를 만들어 이 작업을 수행했다.

## 수행 내용

- 변경 목적: `SG-021` 리뷰가 남긴 결함 중 파일 소유권상 Claude(UI) 몫인 두 건을 최소 수정.
  - **L1** — `src/ui/app.ts`의 document `keydown` 리스너는 `InputController`
    (`src/game/input-controller.ts`, game 소유)가 항상 장착된 production에서
    `stopPropagation`/`stopImmediatePropagation`로 완전히 억제되는 죽은 경로였다.
    사용자 영향은 없지만, 그 억제 계약이 문서화되어 있지 않아 `InputController`의 억제
    호출을 나중에 제거하면 조용히 이중 dispatch가 부활하는 유지보수 함정이었다
    (`AC-U06` 위반 위험). **최소 수정**: `keydownListener` 정의부에 억제 계약과 위험을
    명시하는 주석 추가. 로직 변경 0.
  - **L3** — `src/ui/render.ts:6`, `src/styles.css:3`이 `#board`를 "SG-012가 Phaser
    canvas를 mount **할 예정인** placeholder"로 설명했으나, SG-012는 이미 완료되어
    `src/game/bootstrap.ts`가 실제로 canvas를 그 자리에 mount한다. **최소 수정**: 두
    주석을 현재 상태(이미 mount됨)로 갱신.
- 변경 파일:
  - `src/ui/app.ts` — 주석 10줄 추가, 로직 변경 없음.
  - `src/ui/render.ts` — 주석 1문장 갱신.
  - `src/styles.css` — 헤더 주석 1문장 갱신.
  - `docs/coordination/tasks/SG-022-2.yaml`, `docs/coordination/handoffs/SG-022-2.md` — 이
    packet/handoff.
- 의도적으로 변경하지 않은 범위:
  - `src/game/board-renderer.ts` 등 L2(먹이 획득 윤곽 펄스) — game 소유, 이 packet 밖.
  - `#board` accessible name/role(L5) — 사람이 명시적으로 이번 revision 범위에서 제외(선택
    폴리시, `AC-U08`은 이미 PASS).
  - `README.md`(L4) — SG-025 소유로 SG-021이 이미 분리 지정.
  - `src/main.ts`의 `APP_BOOT_STATUS` scaffold 상수 — L3 원문이 "SG-022 또는 소유자 정리"로
    소유권을 명확히 하지 않았고, 승인된 범위(app.ts/render.ts/styles.css)에도 없어 제외.
  - `src/game/input-controller.ts` — L1의 억제 로직 자체(game 소유)는 손대지 않고, UI 쪽
    문서화만 수행.
  - `tests/**`, domain/game/adapter/config, package/lockfile, build 설정, `STATUS.md`,
    `docs/coordination/tasks/SG-022.yaml`/`handoffs/SG-022.md`(M1 기록) — 전부 미변경.
  - `SG-020`(negative control, `blocked`) 잔여 작업 — 이 packet과 무관, 별도 트랙.

## 수용 기준 증거

| AC | 증거 | 결과 |
|---|---|---|
| SG-022-L3-AC01 | `git show HEAD:src/ui/render.ts \| sed -n '1,7p'`, `git show HEAD:src/styles.css \| sed -n '1,6p'` — 둘 다 "mounts its canvas into/hosts the Phaser-rendered canvas"로 현재형 서술 | PASS |
| SG-022-L1-AC01 | `sed -n '136,146p' src/ui/app.ts` — `keydownListener` 바로 위에 InputController 억제 계약 주석 존재 | PASS |
| SG-022-L1-AC02 / L3-AC02 (회귀 없음) | 아래 "실행한 검증" 표 — 수정 전후 모두 133/133 unit PASS, 로직 diff 0(comment-only) | PASS |

이 두 결함은 SG-021이 "결함"이 아니라 "유지보수 함정"/"문서 위생"으로 분류했고 전용 테스트가
없었으므로(§SG-021 handoff), 수정 전 실패하는 테스트는 없다. 대신 (a) 수정 전 grep으로 결함
문구가 실존함을 확인하고, (b) 수정이 로직을 건드리지 않았음을 전체 suite 무변화로 증명했다.

### 수정 전 결함 존재 확인 (grep, `base_sha` 기준)

```text
$ git show f200d71:src/ui/render.ts | sed -n '6p'
 * a plain, accessible placeholder that SG-012 will mount the Phaser canvas into.
$ git show f200d71:src/styles.css | sed -n '3p'
 * The Phaser board itself (SG-012) is out of scope; #board here is a plain,
$ git show f200d71:src/ui/app.ts | sed -n '136p'
  const keydownListener = (event: KeyboardEvent): void => {
```

(L1은 로직이 아니라 "설명 부재"가 결함이므로 위 줄 자체는 정상 동작하며, 결함은 그 위에
억제 계약 주석이 없었다는 점이다.)

## 실행한 검증

Node v24.18.0 / npm 11.16.0, `nvm use 24` 필요(기본 셸은 Node 22).

| 명령 | exit code | 실제 결과 |
|---|---:|---|
| `npm run format:check` | 0 | All matched files use Prettier code style! |
| `npm run lint` | 0 | 오류·경고 0 |
| `npm run typecheck` | 0 | 오류 0 |
| `npm run test` | 0 | 15 files / 133 tests PASS (35.99s) |
| `npm run build` | 0 | 27 modules; `bootstrap` chunk 1,398.41 kB / gzip 364.92 kB — 기존 baseline과 동일한 500kB 청크 경고만 있음(신규 아님) |
| `git diff --check` | 0 | 공백 오류 0 |

실행하지 못한 검증과 이유: `npm run test:e2e`는 실행하지 않았다. 이 revision은 순수
주석 변경이고 `src/game/**`·`tests/**`를 건드리지 않아 SG-022 M1이 이미 확보한 clean
Chromium 16/16 PASS(base `f200d71`와 코드 동일) 대비 추가로 검증할 렌더링/입력 동작 변화가
없다. 필요 시 재현 가능(리뷰어 재현 절차 참조).

### 리뷰 반영 후 재검증 (`remediation_head_sha` `3d0f7a6`)

| 명령 | exit code | 실제 결과 |
|---|---:|---|
| `npm run format:check` | 0 | All matched files use Prettier code style! |
| `npm run lint` | 0 | 오류·경고 0 |
| `npm run typecheck` | 0 | 오류 0 |
| `npm run test` | 0 | 15 files / 133 tests PASS (7.00s, 이전과 동일) |
| `npm run build` | 0 | 27 modules; 청크 크기 변화 없음 |
| `git diff --check` | 0 | 공백 오류 0 |

## 독립 리뷰

`docs/TASKS.md`의 SG-022 행 규칙("발견자와 수정자가 다르면 발견자, 같으면 제3 플랫폼")에
따라, 이 revision은 발견자(SG-021 리뷰)와 수정자가 모두 Claude이므로 제3 플랫폼 리뷰가
필수였다.

- **1차 리뷰 결과** (`implementation_head_sha` `7af18a0` 대상): 범위·base SHA·소유권·최소
  diff는 모두 적절, QA branch clean으로 평가. **P2 수정 요청 1건**: `src/ui/app.ts`
  주석의 "InputController가 every key를 intercept한다"는 표현이 부정확 — 실제로는
  board handler가 지원하지 않는 키를 통과시키고, document capture도 shell shortcut만
  억제한다. "handled shell shortcuts/commands"로 한정하라는 요청. 그 외에는 승인 가능하다고
  명시.
- **리뷰어 플랫폼**: 채팅 메시지 자체에는 리뷰어 플랫폼이 명시되지 않았다. `AGENTS.md` §5
  소유권 표상 `src/ui/**`의 기본 리뷰어는 Antigravity이므로 그로 추정하나, 이 handoff는
  추정임을 명시적으로 밝힌다.
- **반영**: `remediation_head_sha` `3d0f7a6`에서 주석을 "handled shell shortcut
  command[s]"로 한정하고 이중 dispatch 경고 문장은 유지했다. 전체 필수 검증을 재실행해
  `format:check`/`lint`/`typecheck`/`test`(133/133 unchanged)/`build`/`git diff --check`
  전부 exit 0으로 회귀 없음을 확인했다.
- **판정**: 리뷰어가 "그 외에는 승인 가능"이라고 명시했으므로, 이 P2 수정 반영을 조건부
  승인의 충족으로 보고 상태를 `verified`로 기록한다. 다만 이는 self-reported이며, 리뷰어의
  재확인 없이 곧바로 `main`에 통합하지는 않는다 — 통합은 조정 책임자(Codex) 소관이다.

## 결정과 위험

- 새 의존성: 없음.
- 새 결정 또는 기존 결정과의 차이: 없음. 주석만 갱신했고 공개 계약·AC는 그대로다.
- 알려진 위험/제약:
  - `SG-020`(negative control, `blocked`)과 이 브랜치의 원래 위치(`claude/sg-022-minimal-defect-fix-052c0b`)는 여전히 `main`과 분리된 상태로 남아 있다. 이 packet은 그 통합을 다루지
    않으며, 조정 책임자가 별도로 처리해야 한다.
  - L1 주석은 계약을 "문서화"할 뿐 `InputController`의 억제 로직 자체를 강제하지 않는다.
    game 소유 파일에 동일 계약을 코드로 강제하는 것(예: 억제 여부를 옵션으로 노출)은 이
    packet의 최소 수정 범위를 벗어나 별도 결정이 필요하다.
- 남은 작업과 권장 담당자:
  - 제3 플랫폼(Codex/Antigravity) 독립 리뷰 → 승인 후 조정 책임자가 `main`에 통합.
  - `STATUS.md` 정합화(SG-022 revision 2 반영) → 조정 책임자(Codex) 전용.
  - `SG-020`/이 브랜치 원본(`claude/sg-022-minimal-defect-fix-052c0b`)의 `main` 통합 여부
    판단 → 조정 책임자.
  - L5(선택, `#board` accessible name/role)와 `src/main.ts`의 `APP_BOOT_STATUS` 정리는
    사람이 이번 범위에서 제외했으므로 별도 결정 시 새 packet으로.

## 리뷰어 재현 절차

1. `nvm use 24 && git switch agent/claude/SG-022-ui-defects && npm ci`
   (주 체크아웃 경로에 공백 있음 — 인용 주의).
2. `npm run format:check && npm run lint && npm run typecheck && npm run test && npm run build`
   → 전부 exit 0, unit 133/133.
3. `git diff f200d71..HEAD -- src/ui/app.ts src/ui/render.ts src/styles.css`로 diff가
   주석 3곳뿐이고 로직 변경이 없음을 확인.
4. `sed -n '130,147p' src/ui/app.ts`와 `src/game/input-controller.ts`의
   `stopPropagation`/`stopImmediatePropagation` 호출을 대조해 주석이 실제 억제 경로와
   일치하는지 확인.
5. `git diff --check` → 공백 오류 0.
