# 작업 인계: `SG-015`

## 식별 정보

- 담당 AI / 모델 / 설정: Antigravity / Gemini 3.5 Flash (High) / qa-review-sg-015
- 명세 버전 / task packet revision: `1.0-plan` / revision 1
- 기준 SHA(`base_sha`): `e5d956a4032f1cea7d8f3d00000c074e153435ed`
- 브랜치 / 결과 SHA(`head_sha`): `qa-review-sg-015` / `a5d2d2fa4b94fe27b9767b68a6a6423b6027654f`
- 연결 Issue / PR: `docs/coordination/tasks/SG-015.yaml` 단일 offline 원본
- 승인자 / 승인 시각: human-user / `2026-07-20T03:00:26+09:00`
- 상태: `review-ready`

## 수행 내용

- 변경 목적: 수직 슬라이스에 대한 독립적인 QA 감사를 자동화하고, 320px 레이아웃, 키보드 입력, 다중 입력, 콘솔/네트워크 에러, 20회 재시작 안정성을 검증한다.
- 변경 파일:
  - `tests/e2e/qa-audit.spec.ts`: 320px 레이아웃, 키보드 내비게이션, 포커스 전이, 입력 큐잉(다중 입력), 20회 재시작 동작을 검증하는 독립 Playwright E2E 테스트 스펙.
  - `docs/coordination/tasks/SG-015.yaml`: 오프라인 태스크 정보 및 발견된 결함 기록.
  - `docs/coordination/handoffs/SG-015.md`: 이 문서 (인계 내용 및 QA 검증 결과 기록).
- 의도적으로 변경하지 않은 범위:
  - `src/**` 아래 모든 제품 코드 (제품 코드 수정 금지 규칙 준수).
  - 기존 테스트 및 설정 파일들.

## 수용 기준 증거

| AC | 증거(테스트 이름, 스크린샷, 로그 또는 수동 절차) | 결과 |
|---|---|---|
| `AC-U01` | `Audit 320px layout and scroll behavior` - 320x568 뷰포트에서 horizontal scroll width가 client width 이하이고 scrollX가 0임을 확인. | **PASS** |
| `AC-U09` | `Audit 320px layout and scroll behavior` - 좁은 모바일 화면 및 320px에서 UI 요소의 잘림 및 스크롤 발생이 없음. | **PASS** |
| `AC-U02` | `Audit keyboard input, focus transitions, and scroll prevention` - 키보드 조작 흐름(Start -> Ready -> Pause -> Resume -> Restart) 검증. 단, `m` 키를 이용한 음소거 토글 시 UI 버튼 및 상태가 업데이트되지 않는 결함(`DF-SG015-01`) 발견. | **FAIL (Defect)** |
| `AC-U06` | `Audit keyboard input, focus transitions, and scroll prevention` - 포커스 전이 및 스크롤 방지 동작 확인. | **PASS** |
| `AC-G03` | `Audit multi-input queueing and direction validation` - ArrowUp -> ArrowLeft 연속 입력 시 180도 회전 즉시 사망이 발생하지 않고 입력 큐에 정상적으로 순차 대기 및 처리됨을 확인. | **PASS** |
| `AC-G10` | `Audit 20-time restart reliability` - 20회 연속 재시작 시에도 게임 상태 및 이벤트가 정상 초기화 및 반복 동작하는지 확인. | **PASS** |
| `AC-R06` | `Audit 20-time restart reliability` - 20회 연속 재시작 시 이벤트 리스너 누수나 비정상 속도 가속 없이 안정적으로 동작함을 확인. | **PASS** |
| `AC-R05` | `Audit 320px layout`, `Audit keyboard input`, `Audit 20-time restart` - 테스트 실행 중 브라우저 콘솔 오류, unhandled rejections, 네트워크 자산 404가 발생하지 않음(setupPageListeners 확인). | **PASS** |

## 실행한 검증

| 명령/환경 | exit code | 실제 결과 |
|---|---:|---|
| `npm run verify` / Node 22 | 0 | 포맷, 린트, 타입 체크, 121개 유닛 테스트, 커버리지(97.59%), 빌드 및 7개 E2E 테스트(vertical-flow, scaffold, qa-audit 포함) 모두 통과. |
| `npx playwright test tests/e2e/qa-audit.spec.ts` / Node 22 | 0 | 5개 테스트 전체 통과 (Mute 단축키 검증은 test.fail() 처리되어 expected fail로 정상 통과 처리). |

실행하지 못한 검증과 이유:
- 실기기(iOS/Android)를 이용한 수동 검토는 샌드박스 CLI 환경 제한으로 인해 수행하지 못하였으며, Playwright 모바일 뷰포트 및 터치 에뮬레이션으로 대체 수행함.

## 결정과 위험

- 새 의존성: 없음
- 새 결정 또는 기존 결정과의 차이: 제품 코드에 손을 대지 않고 QA 테스트 코드를 통해 제품의 결함을 독립적으로 감지함.
- 알려진 위험/제약:
  - **결함 `DF-SG015-01` (Medium)**: 키보드 `m` 키를 입력했을 때 `ApplicationRouter` 내부적으로 `toggleMute` 명령은 처리되나, UI 쉘의 로컬 변수인 `muted` 상태를 토글하고 DOM 버튼의 `aria-pressed`나 textContent를 갱신하는 콜백/이벤트 흐름이 누락되어 있음. 이로 인해 키보드 단축키를 통한 음소거가 시각적으로 적용되지 않음.
- 남은 작업과 권장 담당자:
  - Wave 3의 일시정지/음소거 세부 구현 단계(`SG-016`, `SG-017`)에서 UI 쉘과 `ApplicationRouter` 간의 오디오 상태 바인딩 방식을 결정하여 결함 `DF-SG015-01`을 해결해야 함.

## 리뷰어 재현 절차

1. `qa-review-sg-015` 브랜치를 체크아웃합니다.
2. `npm ci`를 통해 의존성을 설치합니다.
3. `npx playwright test tests/e2e/qa-audit.spec.ts`를 실행하여 5개 테스트 전체가 성공(Mute 테스트는 Expected fail로 통과)하는 것을 확인합니다.
4. `tests/e2e/qa-audit.spec.ts` 파일의 구현 내용을 확인하여 레이아웃, 키보드 포커스, 다중 입력 큐, 20회 재시작 시나리오가 올바르게 검증되고 있는지 검토합니다.
