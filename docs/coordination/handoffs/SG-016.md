# 작업 인계: `SG-016`

## 식별 정보

- 담당 AI / 모델 / 설정: Codex / GPT-5 runtime / high.
- 명세 버전 / task packet revision: `1.0-plan` / revision 1.
- 기준 SHA(`base_sha`): `5ba28f663cd4a164f78af42ca6834460cc3c9b40`.
- 브랜치 / 구현 SHA(`head_sha`): `agent/codex/SG-016-lifecycle-storage-audio` /
  `a94d4eb31d0cf468e7889ede68f3e26e57d0fdc8`.
- 연결 Issue / PR: `docs/coordination/tasks/SG-016.yaml` 단일 offline 원본.
- 승인자 / 승인 시각: human-user / `2026-07-20T07:45:04Z`.
- 상태: `review-ready`; 지정 Claude Sonnet 5 high 독립 리뷰가 남아 있으며 merge, push, 배포는 수행하지 않았다.

## 수행 내용

- 변경 목적: 순수 domain과 DOM UI를 변경하지 않고 lifecycle pause, versioned localStorage preferences/high scores, and optional Web Audio feedback을 browser adapter로 연결한다.
- 변경 파일:
  - `src/game/lifecycle-controller.ts`, `src/game/lifecycle-controller.test.ts`: blur, hidden, actual orientation change는 pause callback으로, resize는 relayout-only로 분리하고 listener teardown을 검증한다.
  - `src/adapters/game-preferences.ts`, test: `snake-game:v1:high-score:<difficulty>` key별 high score, mute, last difficulty를 JSON validation/try-catch와 memory fallback으로 구현한다.
  - `src/adapters/audio-feedback.ts`, test: user-command 이후에만 AudioContext를 만들고 food/end/win short tone을 내며 mute, constructor/resume/play failure를 no-op으로 격리한다.
  - `src/game/bootstrap.ts`: storage/audio가 snapshots/events를 소비하고 모든 board/shell command가 같은 gesture-aware dispatch를 지나도록 조립한다.
  - `src/game/application-router.test.ts`: pause의 current direction preservation, queue/accumulator clear, paused input ignore, explicit resume의 새 tick wait를 보강한다.
  - packet, STATUS, this handoff: scope/claim/evidence를 기록한다.
- 의도적으로 변경하지 않은 범위: `src/domain/**`, `src/config/**`, `src/ui/**`, styles, main, E2E, package/lockfile, build/test configuration, accepted plan/contracts/decisions, deployment artifacts.

## 수용 기준 증거

| AC | 증거 | 결과 |
|---|---|---|
| `AC-L01`, `SG-016-AC01` | `LifecycleController` unit test: resize 2회는 relayout만, blur + hidden + portrait→landscape는 pause, same-family orientation event와 hidden→visible은 resume을 만들지 않음. | PASS |
| `AC-L02`, `AC-L03`, `SG-016-AC02` | `ApplicationRouter` test: pause keeps `direction`, clears two-entry queue/accumulator, paused direction and delta are ignored; resume begins at accumulator 0 and `tickMs - 1` does not move. | PASS |
| `AC-R01`, `SG-016-AC03/04` | `GamePreferences` test: v1+difficulty keys keep slow/normal scores split; malformed JSON, infinity, invalid mute/difficulty default safely; SecurityError/QuotaExceededError leave memory state and router progress usable. | PASS |
| `AC-R02`, `SG-016-AC05` | `AudioFeedback` test: no factory call before gesture; one pending activation makes one context; mute suppresses tone; throwing constructor and rejected resume produce no unhandled failure. | PASS |

## 실행한 검증

Node `v24.14.0` runtime path was prepended for verification. A prior sandbox-only `npm run test:e2e` failed before test start because localhost bind returned `EPERM`; the same suite was then executed in a persistent localhost-approved terminal session and completed.

| 명령/환경 | exit code | 실제 결과 |
|---|---:|---|
| `npm ci` | 0 | lockfile 변경 없이 의존성 설치. Shell Node 22 emitted only the expected engine warning; all listed verification commands used bundled Node 24.14.0. |
| `npm run format:check` | 0 | Prettier PASS. |
| `npm run lint` | 0 | ESLint `--max-warnings 0` PASS. |
| `npm run typecheck` | 0 | strict TypeScript PASS. |
| `npm run test` | 0 | 12 files, 128 tests PASS. |
| `npm run build` | 0 | Vite production build PASS; existing Phaser >500 kB chunk warning only. |
| `npm run test:e2e` / Chromium, localhost-approved persistent terminal | 0 | production suite 7/7 PASS in 2.6m. |
| `git diff --check` | 0 | whitespace errors 0 before implementation commit. |

## 결정과 위험

- 새 의존성: 없음.
- 새 결정 또는 기존 결정과의 차이: 없음. `GameState` remains free of storage/audio state; adapters only consume command, snapshot, and domain event boundaries.
- 알려진 위험/제약: existing `DF-SG015-01` keyboard mute visual state remains intentionally assigned to SG-017's `src/ui/**` scope. SG-016 makes keyboard mute affect the audio preference/persistence but does not alter UI ownership.
- 남은 작업과 권장 담당자: Claude Sonnet 5 high independently reviews scope, browser adapter failure boundaries, and command wiring. After approval, Codex may integrate; SG-017 binds persisted best/mute state to the DOM shell.

## 리뷰어 재현 절차

1. Check out `a94d4eb31d0cf468e7889ede68f3e26e57d0fdc8` on `agent/codex/SG-016-lifecycle-storage-audio` with Node 24.
2. Run `npm run format:check`, `npm run lint`, `npm run typecheck`, `npm run test`, `npm run build`, and `npm run test:e2e` (localhost permission required for Playwright preview).
3. Confirm the diff is limited to the packet/handoff/STATUS, `src/adapters/**`, and listed `src/game/**`; verify no domain, UI, config, dependency, or accepted-contract file changes.
