# 작업 인계: `SG-019`

## 식별 정보

- 담당 AI / 모델 / 설정: Codex / GPT-5 runtime / high
- 명세 버전 / task packet revision: `1.0-plan` / revision 1
- 기준 SHA(`base_sha`): `74fe266702053bdab44718466cd5f7a99d1035fc`
- 브랜치 / 구현 SHA(`head_sha`): `agent/codex/SG-019-feature-integration` /
  `583c7342810d010f47d846bec22c22446b29c523`
- 연결 Issue / PR: `docs/coordination/tasks/SG-019.yaml` 단일 offline 원본
- 승인자 / 승인 시각: human-user / `2026-07-23T08:07:03Z`
- 상태: `blocked` — clean root-base 전체 verify는 PASS했지만 Pages-base 전체 E2E의
  첫 테스트에서 SG-018 소유 flaky scroll observation이 1회 실패했다. 동일 focused
  재현은 PASS했으며 `DF-SG019-01`로 원 소유자 Antigravity에 반환했다.

## 수행 내용

- 변경 목적:
  - 저장된 난이도에서 simulation과 UI가 함께 시작하도록
    preferences → ApplicationRouter → snapshot → DOM 흐름을 완성한다.
  - UI가 복제한 초기 `GameState`와 실제 진입점이 아닌 `dispatchStub` 경로를 제거해
    DOM command → router → domain → snapshot/event → Phaser/UI 단방향만 남긴다.
  - `gameEnded` event의 self 충돌 셀 또는 wall 인접 경계를 renderer가 재판정 없이
    약 300ms 윤곽으로 표시한다.
- 변경 파일:
  - `src/game/bootstrap.ts`: persisted `lastDifficulty`로 router 초기화.
  - `src/ui/render.ts`: snapshot difficulty로 radio checked 상태 렌더.
  - `src/ui/app.ts`, `src/ui/state.ts`, `src/ui/state.test.ts`: 수동 초기
    `GameState` fixture 제거, 첫 실제 application snapshot만 소비.
  - `src/ui/main.ts`: 사용되지 않는 pre-integration `dispatchStub` 진입점 삭제.
  - `src/game/collision-marker.ts`와 test: domain `gameEnded` 좌표를 self cell/wall
    edge 표시 geometry로 변환.
  - `src/game/board-renderer.ts`와 test: 별도 overlay, 300ms clear, destroy cleanup.
  - `tests/e2e/production.spec.ts`: Slow 선택과 서로 다른 best score가 reload 뒤
    simulation snapshot과 radio/HUD에 복원되는 production 회귀 증거.
  - SG-019 packet, handoff, STATUS.
- 의도적으로 변경하지 않은 범위:
  - `src/domain/**`, 공개 계약과 제품 의미, 설정/의존성/lockfile, CI/Pages workflow,
    styles, `src/main.ts`, 기존 SG-001~018 packet/handoff, 생성물 `dist/**`.
  - SG-018 scroll 테스트는 `DF-SG019-01`의 원 소유자 범위로 반환했으며 이 patch에서
    retry/sleep/완화/수정을 하지 않았다.

## 수용 기준 증거

| AC | 증거 | 결과 |
|---|---|---|
| SG-019-AC01 | SG-016~018 packet/handoff와 base/head/review/integration ancestry 확인. SG-017 chat-relayed 리뷰 예외는 현재 사람이 직접 수용했고 SG-018 Codex APPROVE 정합화 SHA `74fe266`을 base로 사용 | PASS |
| SG-019-AC02 | `INITIAL_MENU_SNAPSHOT`과 `src/ui/main.ts` stub 제거. production에는 `src/main.ts` → bootstrap → Input/Lifecycle/UI command → ApplicationRouter → domain → snapshot/event → Phaser/UI/adapters 경로만 존재 | PASS |
| SG-019-AC03 / AC-R01 | 수정 전 focused production E2E는 reload 뒤 Slow radio가 unchecked라 exit 1. `583c734`에서는 persisted Slow + Best 20 reload가 1/1 PASS | PASS |
| SG-019-AC03 / AC-G07 / AC-U04 | `collisionMarkerFor`가 self C(2,3)과 네 wall edge를 검증하고 BoardRenderer test가 right edge `(478,242)→(478,262)` 및 300ms overlay clear를 검증 | PASS |
| SG-019-AC04 root | clean clone `583c734`의 `npm run verify`: format/lint/typecheck, 14 files/132 unit, coverage, build, root Chromium 16/16 | PASS |
| SG-019-AC04 Pages | clean Pages-base 전체 E2E 첫 테스트가 scrollY 40→40으로 실패. 동일 focused rerun은 1/1 PASS | BLOCKED — DF-SG019-01 |
| SG-019-AC05 | root와 Pages-base production build 모두 생성 가능하고 제품 smoke는 root 16/16에서 통과. H2 사람 판정은 수행·주장하지 않음 | CANDIDATE, gate pending |

## 실행한 검증

Node `v24.14.0`, npm `11.18.0`, Playwright `1.61.1` Chromium을 사용했다.

| 명령/환경 | exit code | 실제 결과 |
|---|---:|---|
| `npm ci` / clean clone | 0 | 157 packages 설치, manifest/lockfile diff 0 |
| `npm run verify` / clean root-base clone | 0 | format, lint, typecheck, 14 files/132 tests, coverage 97.59% stmts·96.61% branches·100% funcs, build, Chromium 16/16 |
| `PLAYWRIGHT_BASE_PATH=/snake-game/ npm run test:e2e` / clean clone | 1 | 첫 keyboard/scroll test에서 Space 후 scrollY가 40보다 커지지 않음. 지시대로 suite 중단: 3 PASS, 1 product-unrelated interrupted, 11 not run |
| 같은 Pages-base command + failing-test `--grep` | 0 | 동일 SHA focused reproduction 1/1 PASS, 14.7s |
| lastDifficulty focused E2E / 수정 전 | 1 | reload 뒤 Slow radio unchecked |
| lastDifficulty focused E2E / 수정 후 | 0 | persisted Slow/Best 20 reload 1/1 PASS |
| `git diff --check 74fe266..583c734` | 0 | whitespace 오류 0 |
| generated-artifact audit | 0 | `dist`, coverage, Playwright report/result 추적 파일 0 |

실행하지 못한 검증과 이유:

- Pages-base 3회 연속 16/16은 `DF-SG019-01` 안정화 또는 적합한 display-backed
  runner 없이는 완료 증거로 만들 수 없다.
- 지정 Claude Opus 4.8 독립 리뷰는 아래 기록처럼 완료됐고 코드 관점 APPROVE다.
- H2는 사람 플레이테스트 관문이며 이 작업이 대신 승인하지 않는다.

## 결정과 위험

- 새 의존성: 없음.
- 새 결정 또는 기존 결정과의 차이: 없음. 저장 난이도는 이미 SG-016 adapter와
  SG-017 handoff에 존재한 값이며 이번 변경은 누락된 production 배선만 완성한다.
  충돌 marker는 domain event의 `reason/headCell/attemptedCell`만 사용한다.
- 알려진 위험/제약:
  - `DF-SG019-01`은 제품 사용자 영향은 없지만 P0 통합 gate를 비결정적으로 막는
    medium 테스트 안정성 결함이다.
  - collision overlay의 실제 250ms/400ms 다중 브라우저 시각 증거는 계획대로
    SG-020 수동/시각 QA 범위다. 이번 단위 증거는 geometry와 300ms timer 경계를
    검증한다.
  - 기존 Phaser production chunk 500kB 경고는 유지되며 새 경고가 아니다.
- 남은 작업과 권장 담당자:
  1. Antigravity: packet의 서로 구분된 line 266 scroll observation과 line 661
     detached-macOS CVDisplayLink 증거를 재현·정합화하고, sleep/retry 없이
     Pages-base 전체 16/16을 3회 연속 제출.
  2. Codex: 수정 head를 재검토하고 clean root/Pages-base 전체 verify 재실행.
  3. 사람: 통과 artifact로 H2 플레이테스트 판정. Pages-base 필수 증거를 면제하려면
     별도의 명시적 예외 승인이 필요하다.

## 리뷰어 재현 절차

1. `583c7342810d010f47d846bec22c22446b29c523`을 clean checkout하고 Node 24/npm
   11.18.0에서 `npm ci`, `npm run verify`를 실행한다.
2. `PLAYWRIGHT_BASE_PATH=/snake-game/ npm run test:e2e`로
   `DF-SG019-01`의 full-suite flake를 확인하고, 실패 시 첫 테스트 trace의
   `tests/e2e/production.spec.ts:266` scrollY 값을 비교한다.
3. `git diff --name-status 74fe266..583c734`로 변경이 packet 허용 경로 안인지
   확인하고, `src/domain/**`, package/lockfile/config/accepted 계약 diff가 0인지
   확인한다.
4. `src/game/collision-marker.test.ts`와 `board-renderer.test.ts`에서 self/wall
   geometry와 300ms overlay clear가 event payload만 사용하는지 검토한다.

## Claude Opus 4.8 독립 리뷰 (`2026-07-24`)

- 담당 AI / 모델: Claude / Opus 4.8 / high
- 리뷰 대상: base `74fe266` → head `583c734`
- 판정: **코드 관점 APPROVE.** `DF-SG019-01`은 SG-019 회귀가 아니라 이 실행
  환경의 E2E 하네스 결함으로 확정.

### 환경 비의존 검증 (재실행, Node `v24.18.0`)

| 단계 | 결과 |
|---|---|
| format:check / lint / typecheck | PASS (exit 0) |
| unit | 14 files / 132 passed |
| coverage | 97.59% stmts / 96.61% branches / 100% funcs (핸드오프와 일치) |
| build | PASS |

### `DF-SG019-01` 근본 원인 분석 (환경, SG-019 무관)

- root-base 전체 E2E에서 `production.spec.ts:661`(느림/보통 난이도 지속성)이
  실패. 단독 3회 재현 → 3/3 실패. 증상은 스네이크가 벽에 닿아도 phase가
  `Playing`에 정지, `Score 0`으로 시뮬레이션 틱이 진행되지 않음. 콘솔/페이지
  JS 오류는 0.
- **base `74fe266`를 별도 워크트리로 checkout해 동일 테스트를 실행한 결과 base도
  똑같이 line 710 `Playing` 정지로 실패**했고, 러너 로그에 원인이 노출됨:
  `CVDisplayLinkCreateWithCGDisplay failed. CVReturn: -6670`.
- 즉 이 macOS 세션에서 Phaser WEBGL의 게임 루프(requestAnimationFrame)가
  CVDisplayLink 실패로 구동되지 못해, Phaser 틱에 의존하는 모든 E2E가 얼어붙음.
  base에서도 재현되므로 SG-019 patch가 도입한 결함이 아니며, "Pages-base scroll
  observation" 역시 이 광범위한 환경 flakiness의 한 발현일 뿐 Pages-base 한정이나
  SG-019 원인이 아님.
- 코드 검토: `collision-marker.ts`(도메인 권위 좌표만 표현 지오메트리로 매핑,
  6케이스 단위 테스트), `board-renderer.ts`(별도 overlay 레이어·300ms clear·
  destroy 정리, `import type Phaser`로 런타임 의존 제거), `INITIAL_MENU_SNAPSHOT`
  및 `src/ui/main.ts` 제거(진입점은 `src/main.ts`, `subscribe` 동기 방출로 최초
  스냅샷 확보) 모두 계약 경계를 지키며 건전.

### 안정화 권고 (Antigravity)

- 실제 필요한 조치는 "scroll assertion의 sleep/retry 제거"보다, **E2E가
  CVDisplayLink/활성 디스플레이 없이도 Phaser 틱을 결정적으로 구동하도록 하는
  것**(headed/디스플레이 보장 러너, 또는 틱을 시간·rAF 비의존으로 주입). 그렇지
  않으면 display-less/detached 세션에서 Pages-base 3회 연속 16/16 증거를 만들 수
  없음.

### Codex 통합 판정

- 리뷰 commit `d52e5ba10face92b760a86838090599e489b5faf`은 현재 SG-019 handoff의
  직접 자식이며, 승인 경로인 이 파일에 44줄만 추가했다. 리뷰 범위도 packet의
  base `74fe266` → implementation `583c734`와 정확히 일치하므로 지정 리뷰 증거로
  수용한다.
- Claude가 base와 implementation에서 함께 재현한 line 661 Phaser freeze는
  SG-019 회귀가 아닌 환경 결함으로 격리됐다. 다만 Codex가 먼저 관찰한 필수
  Pages-base 실패는 line 266의 scrollY 40→40이고 focused rerun은 통과했다.
  두 증상의 인과관계는 입증되지 않았으므로 같은 근본 원인으로 합치지 않는다.
- 따라서 코드 리뷰 관문은 PASS지만 `SG-019-AC04`의 Pages-base full-suite 증거는
  여전히 BLOCKED다. display-backed 환경에서 요구 증거를 만들거나 사람이 이
  필수 검증 편차를 명시적으로 수용하기 전에는 main 병합·H2 후보 완료로 올리지
  않는다.
