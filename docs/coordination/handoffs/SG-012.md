# 작업 인계: `SG-012`

## 식별 정보

- 담당 AI / 모델 / 설정: Codex / current GPT-5 runtime (정확한 alias 미노출) / high.
- 명세 버전 / task packet revision: `1.0-plan` / revision 1.
- 기준 SHA(`base_sha`): `cf811f293918a4b7eed38b5cb44c3673950959cb`.
- 브랜치 / 구현 SHA(`head_sha`): `agent/codex/SG-012-phaser-integration` / `3393cb765deaafdc6bdbdf0f6d2d99b0a667e1b3`.
- 연결 Issue / PR: `docs/coordination/tasks/SG-012.yaml` 단일 offline 원본; GitHub Issue/PR 없음.
- 승인자 / 승인 시각: Codex integrator / `2026-07-19T00:00:00Z`, 사람의 직접 승인 지시를 근거로 한다.
- 상태: `merged`. 지정 독립 Claude Sonnet 5 review 3차가 재제출 SHA
  `3393cb765deaafdc6bdbdf0f6d2d99b0a667e1b3`를 `APPROVE`했고, Codex integrator가
  `ef76675a2ea6f0aaad16e3c5d0814ab4645c68d7` 위에 integration commit
  `05cddcd3dbfea9eab6d8319911900580474630b4`로 병합했다. SG-014 preflight에서 해당 구현·리뷰·
  merge SHA가 모두 `main` 조상이며 `origin/main`도 같은 merge SHA임을 재확인했다. PR과 공개
  배포는 수행하지 않았다.

## 수행 내용

- 변경 목적: 단일 Phaser `GameScene`과 얇은 application adapter를 추가했다. 순수 domain의 `reset`, `enqueueDirection`, `step`만 호출하고 점수·충돌·성장·속도 규칙은 Scene에서 다시 구현하지 않는다.
- 변경 파일:
  - `src/game/application-router.ts`, `fixed-step-scheduler.ts`: semantic command routing, 250ms delta clamp, frame당 최대 3 step, pre-step `stepDuration` capture와 pause/resume accumulator clear.
  - `src/game/game-scene.ts`, `board-renderer.ts`, `bootstrap.ts`: 480×480 / 20×20 Phaser Graphics board, FIT/center, single Scene mount와 snapshot/event rendering.
  - `src/game/input-controller.ts`, `lifecycle-controller.ts`: board-focus keyboard, same command route의 DOM button dispatch, hidden/blur/orientation pause, resize relayout과 cleanup.
  - `src/game/*test.ts`: scheduler duration/cap/clamp, READY Right, pause accumulator/queue, 20 restart의 subscriber baseline을 검증한다.
  - `src/main.ts`, `index.html`: browser에서 Phaser를 dynamic import하는 production entry.
  - `tsconfig.json`: TypeScript 6과 Phaser 4.2.1의 설치된 declaration 두 곳 호환성 오류만 건너뛰도록 `skipLibCheck: true`를 추가했다. project source 타입 검사는 유지한다.
- 의도적으로 변경하지 않은 범위: `src/domain/**`, `src/config/**`, `src/ui/**`, CSS, package/lockfile, tests/, accepted 계약·결정·상태 문서는 변경하지 않았다. `dist/`와 test artifact도 추적하지 않았다.

## 수용 기준 증거

| AC | 증거(테스트 이름, 로그 또는 수동 절차) | 결과 |
|---|---|---|
| `SG-012-AC01` | `ApplicationRouter`가 domain public API만 import하며 scheduler가 `step` result를 publish한다. Scene/renderer에는 game-rule branch가 없다. | PASS |
| `SG-012-AC02`, `AC-G04` | `FixedStepScheduler` tests의 `clamps delta and advances at most three fixed steps`와 `subtracts the duration captured before a speed-changing step`. | PASS |
| `SG-012-AC03` | 60ms 누적 뒤 250ms frame에서 `160ms`를 먼저 빼고 다음 `140ms` 속도를 사용해 accumulator `10ms`를 확인한다. | PASS |
| `SG-012-AC04` | `BOARD_LOGICAL_SIZE=480`, `BOARD_CELL_COUNT=20`; production browser에서 Start 후 canvas 1개, hidden=false, intrinsic `480×480`을 확인했다. | PASS |
| `SG-012-AC05`, `AC-G02` | DOM shell buttons와 focused-board `InputController`는 모두 `ApplicationRouter.dispatch` 한 경로를 사용한다. router test가 READY Right의 `playing` 전이와 `['right']` 예약을 확인한다. | PASS |
| `SG-012-AC06`, `AC-L01~L03` | `LifecycleController`는 hidden/blur/portrait↔landscape만 pause callback으로 보내며 resize는 `game.scale.refresh()`만 호출한다. router pause/resume test가 queue/accumulator 0과 paused delta no-op을 확인한다. | PASS |
| `SG-012-AC07`, `AC-G10` | `keeps a single subscriber across twenty restart cycles`가 20 terminal→restart cycle마다 listener count `1`을 확인한다. | PASS |
| `SG-012-AC08` | 설치된 Phaser 4.2.1 declarations에서 TypeScript 6의 두 library-only 오류를 재현했고, `skipLibCheck` 후 strict project `typecheck`가 통과했다. | PASS |

## 실행한 검증

| 명령/환경 | exit code | 실제 결과 |
|---|---:|---|
| Node 24.14.0 `npm ci` | 0 | 139 packages 설치; package/lockfile 변경 0. |
| `npm run format:check` | 0 | Prettier PASS. |
| `npm run lint` | 0 | ESLint `--max-warnings 0` PASS. |
| `npm run typecheck` | 0 | strict TypeScript project source PASS. |
| `npm run test` | 0 | Vitest 7 files / 103 tests PASS. |
| `npm run build` | 0 | Vite production build PASS; Phaser dynamic chunk gzip 363.48 kB, 기존 500 kB warning 발생. |
| `npm run test:e2e` / Chromium | 0 | production preview smoke 1/1 PASS; browser/asset failure 없음. |
| production browser 수동 확인 | PASS | Start 후 READY status·D-pad, canvas 1개, `480×480`, board visible을 확인했다. |
| `git diff --check` | 0 | whitespace 오류 0. |
| Ruby YAML packet field validation | 0 | revision, approval, base, scope, AC, command, claim 필드 parse PASS. |

실행하지 못한 검증과 이유:

- 지정 Claude Sonnet 5 독립 리뷰 3차가 독립 worktree에서 완료되어 `APPROVE`와 `verified` 전환을 권장했다.

## 결정과 위험

- 새 의존성: 없음. `package.json`과 lockfile 변경 없음.
- 새 결정 또는 기존 결정과의 차이: 없음. Phaser는 accepted D-001의 정확한 `4.2.1`과 설치된 type metadata를 사용했다.
- 알려진 위험/제약:
  - Phaser 4.2.1 declaration은 TypeScript 6에서 library-only 두 오류를 내므로 `skipLibCheck`가 필요하다. application source의 strict check는 계속 실행한다.
  - Vite가 Phaser dynamic chunk에 대해 500 kB warning을 출력한다. build는 성공하며 chunk splitting의 별도 기준 변경은 하지 않았다.
  - 기존 DOM shell의 document key listener는 변경하지 않았다. SG-012 `InputController`가 capture 단계에서 board 밖 shortcut routing을 막고 board handler에서 propagation을 중단해 중복 command를 방지한다.
- 남은 작업과 권장 담당자: SG-014가 이미 병합된 Scene/router와 SG-013 DOM shell의 vertical
  production E2E를 추가하고 전체 clean verify를 수행한다.

## 리뷰어 재현 절차

1. `agent/codex/SG-012-phaser-integration`에서 implementation SHA `3393cb765deaafdc6bdbdf0f6d2d99b0a667e1b3`을 checkout한다.
2. Node 24에서 `npm ci`, 여섯 required npm commands, `git diff --check`를 실행한다.
3. production preview에서 Start를 클릭해 READY status, visible 480×480 canvas, D-pad를 확인하고 board focus에서 Right를 한 번 보내 첫 fixed tick을 확인한다.
4. `fixed-step-scheduler.test.ts`의 speed-boundary assertion, `application-router.test.ts`의 20 restart assertion, `LifecycleController`의 pause/resize 분기를 `CONTRACTS.md` §7과 대조한다.

## 리뷰 대응

- 수정 SHA: `01946e71fa939d5ed8db98466e0f1e9794087655` (`fix(SG-012): refresh scale after board reveal`).
- 최소 수정: bootstrap이 `#board`를 찾은 뒤 application snapshot을 구독한다. shell이 먼저 menu
  `hidden` 속성을 제거하고, 그 직후 `menu → ready`의 hidden→visible 전이에서만
  `game.scale.refresh()`를 호출한다. 이는 reviewer가 window resize로 정상화를 확인한 동일 ScaleManager
  경로이며, 일반 resize가 pause를 만들지 않는 기존 lifecycle 계약은 바꾸지 않는다.
- 수정 후 Node 24.14.0에서 `format:check`, `lint`, `typecheck`, `test`(103/103), `build`,
  `test:e2e`(Chromium 1/1), `git diff --check`를 모두 재실행해 PASS했다.
- 상태: `review` 유지. 구현 환경에서는 fresh independent browser의 Start 직후 CSS canvas size를 새로
  수집하지 않았으므로, reviewer가 창 리사이즈 없이 `#board canvas`의 non-zero CSS width/height와
  grid·snake·food를 확인한 뒤에만 `verified`를 권고한다.

## 독립 리뷰

- 판정: **REQUEST_CHANGES — Start 직후 게임판이 화면에 렌더링되지 않는 차단 결함 1건.**
- 검토 대상: packet의 구현 SHA `a350cc8cf8858684dfbbe1f5ef3fee42c9f46596`.
- 독립 환경: Node `22.18.0` (packet 요구 Node 24와 편차, 결함 발견에는 영향 없음). `npm ci`,
  `format:check`, `lint`, `typecheck`, `test`(103/103), `build`, `test:e2e`(Chromium 1/1),
  `git diff --check` 모두 독립적으로 재실행해 전부 PASS했다. diffstat으로 allowed_paths 밖 변경이
  없음을 확인했다.
- 계약 대조: `application-router.ts`, `fixed-step-scheduler.ts`, `board-renderer.ts`,
  `game-scene.ts`, `input-controller.ts`, `lifecycle-controller.ts`를 `CONTRACTS.md` §2~§9와
  대조했다. domain 함수만 호출하는 경계, pre-step `stepDuration` capture, pause/resume에서의
  accumulator/queue clear, keyboard·버튼의 단일 `dispatch` 경로 공유는 코드 리딩상 계약과
  일치한다.
- 차단 결함 (Blocking):
  - `npm run preview` production build를 Chromium에서 열어 Start를 클릭하면 phase는 `Ready`로
    정상 전이하지만 게임판은 빈 사각형만 보이고 grid·snake·food가 전혀 그려지지 않는다. 새로고침
    뒤 재시도해도 100% 재현된다.
  - 원인: `src/ui/render.ts`의 `setHidden(elements.board, isMenu)`가 menu phase에서 `#board`에
    `hidden`(= `display:none`)을 건다. `src/game/bootstrap.ts`의
    `new Phaser.Game({ scale: { parent: board, mode: FIT, ... } })`는 mount 시점(=아직 menu,
    `board.hidden === true`)에 즉시 생성되므로 Phaser FIT ScaleManager가 0×0 부모 기준으로
    캔버스 인라인 스타일을 `width:0px; height:0px`로 고정한다. 이후 phase가 바뀌어 board가
    보여져도 재계산 트리거가 없다. `game.scale.refresh()`는
    `src/game/lifecycle-controller.ts`의 `window resize` 이벤트에만 연결돼 있어(AC-L03 relayout
    경로), 사용자가 창 크기를 바꾸지 않는 한 게임판은 영구히 보이지 않는다.
  - 재현 확정: 브라우저 콘솔에서 `document.querySelector('#board canvas').getAttribute('style')`가
    Start 직후에도 계속 `width: 0px; height: 0px`를 반환했고, `window.dispatchEvent(new
    Event('resize'))`를 수동 주입하자 즉시 `width: 200px; height: 200px`로 정상화되어 원인을
    확정했다.
  - 영향받는 수용 기준: `SG-012-AC04`("board rendering is 480x480... "). handoff의 리뷰어 재현
    절차 3번("visible 480×480 canvas... 확인")과도 모순된다.
  - 왜 기존 검증이 놓쳤는가: `tests/e2e/scaffold.spec.ts`는 Start를 클릭하지 않고 menu 화면에서
    `#board`가 `hidden`인 것만 확인하며 Vitest 단위 테스트는 DOM/CSS 레이아웃을 검사하지 않는다.
    handoff의 수동 확인(hidden=false, intrinsic 480×480)은 canvas 속성만 본 것으로 보이며 실제
    렌더 크기(CSS style width/height)는 확인하지 않은 것으로 보인다.
  - 권장 수정: `#board`가 `hidden`을 벗어나는 시점(예: `start`/`restart` dispatch 직후, 또는
    `#board`에 대한 `ResizeObserver`)에 `game.scale.refresh()`를 호출하도록 연결한다.
- 비차단 권고: 없음.
- 다음 단계: 위 결함을 수정한 뒤 창 리사이즈 없이 Start 직후 canvas가 렌더링되는 브라우저 증거를
  다시 제출한다. 상태는 `review`를 유지하며 `verified`로 전환하지 않는다.

## 독립 리뷰 — 2차 (재제출 검토)

- 판정: **REQUEST_CHANGES 유지 — 수정이 결함을 해소하지 못한다.**
- 검토 대상: 재제출 SHA `01946e71fa939d5ed8db98466e0f1e9794087655`(`fix(SG-012): refresh scale
  after board reveal`), 문서 커밋 `d998b314c8febab2676b2f77e8ed5e725a4ffd87`.
- 독립 환경: `git worktree add`로 `d998b31`을 별도 checkout해 Node 22.18.0에서 `npm ci`,
  `format:check`, `lint`, `typecheck`, `test`(103/103), `build`, `git diff --check`를 재실행했고
  전부 PASS했다 — 이 부분은 구현 기록과 일치한다.
- 브라우저 재현 (차단 결함 재현됨): 새 tab에서 fresh preview build를 열고 리사이즈를 전혀 하지 않은
  채 Start를 클릭했다. phase는 `Ready`로 전이하지만 `#board canvas`의 style은 클릭 직후와 1초 대기
  후 모두 계속 `width: 0px; height: 0px`였고 화면도 빈 사각형이었다 — 1차 리뷰와 동일한 증상이 그대로
  재현된다.
- 근본 원인 진단: 수정은 옳은 트리거(menu→ready의 hidden→visible 전이)를 선택했지만
  `game.scale.refresh()`를 `shell.applySnapshot(state, events)` 직후, 즉 `#board.hidden = false`를
  적용한 것과 **같은 동기 콜백 안에서** 즉시 호출한다. 이 시점에는 브라우저가 아직 해당 DOM 변경에
  대한 layout을 커밋하지 않았거나 Phaser ScaleManager의 부모 크기 재측정이 그 값을 반영하지 못해
  `refresh()`가 여전히 0×0을 관찰한다. 같은 페이지·같은 broken 상태에서 별도로
  `window.dispatchEvent(new Event('resize'))`를 (다른 tick에서) 주입하면 — 즉 정확히 같은
  `LifecycleController.onResize → game.scale.refresh()` 경로인데 시점만 나중이면 — 즉시
  `width: 200px; height: 200px`로 정상화됨을 확인했다. 따라서 `game.scale.refresh()` 자체는 유효한
  수정 방향이지만, **호출 시점이 board가 실제로 visible해진 뒤 레이아웃이 커밋된 다음이어야 한다.**
- 권장 수정: `hidden→visible` 전이를 감지한 직후 `game.scale.refresh()`를 동기 호출하지 말고, 다음
  중 하나로 한 프레임 이상 지연한다.
  - `requestAnimationFrame(() => game?.scale.refresh())`로 다음 페인트 이후에 호출한다.
  - `#board`에 대한 `ResizeObserver`를 달아 브라우저가 실제로 크기 변화를 관찰했을 때 콜백에서
    `refresh()`를 호출한다(가장 견고함 — hidden→visible 전이뿐 아니라 다른 레이아웃 변화도 커버).
  - 최소한 `setTimeout(() => game?.scale.refresh(), 0)`으로 현재 동기 실행 컨텍스트 밖으로 미룬다.
- 영향받는 수용 기준: `SG-012-AC04` 위반이 계속된다.
- 다음 단계: 위 타이밍 수정 후 창 리사이즈 없이 Start 직후 canvas의 CSS width/height가 즉시
  non-zero임을 다시 확인해 제출한다. 상태는 `review`를 유지한다.

## 리뷰 대응 — 3차 제출

- 수정 SHA: `3393cb765deaafdc6bdbdf0f6d2d99b0a667e1b3` (`fix(SG-012): mount Phaser after board layout`).
- Phaser Game/Scene을 menu에서 생성하지 않는다. shell이 Start로 board를 visible 상태로 렌더한 뒤
  다음 task에서 처음 생성하므로 FIT는 `display:none` parent를 측정하지 않는다. pending mount는
  destroy에서 취소하며 일반 resize는 기존대로 relayout만 한다.
- fresh Chromium production preview에서 창 리사이즈 없이 Start만 클릭해 확인했다: canvas 1개,
  `boardHidden: false`, intrinsic `480×480`, inline CSS `width: 200px; height: 200px`; 화면에는
  20×20 grid, 길이 3 snake, food가 모두 렌더링됐다.
- Node 24.14.0 `format:check`, `lint`, `typecheck`, `test`(103/103), `build`, `test:e2e`(Chromium
  1/1), `git diff --check`가 모두 PASS했다. 상태는 독립 최종 판정 전까지 `review`를 유지한다.

## 독립 리뷰 — 3차 (최종)

- 판정: **APPROVE — 차단 결함 0건, `verified` 권장.**
- 검토 대상: 최종 수정 SHA `3393cb765deaafdc6bdbdf0f6d2d99b0a667e1b3`
  (`fix(SG-012): mount Phaser after board layout`), 문서 커밋
  `b5bcb5b36c96e9fb6e5cb8e70dc4b381a178b8bf`(`docs(SG-012): resubmit delayed mount fix`).
- 독립 환경: `git worktree add`로 별도 checkout해 Node 22.18.0에서 `npm ci`, `format:check`,
  `lint`, `typecheck`, `test`(103/103), `build`, `git diff --check`를 재실행했고 전부 PASS했다.
- 수정 접근 검토: `src/game/bootstrap.ts`가 mount 시점에 `Phaser.Game`을 즉시 생성하지 않도록
  바뀌었다. 대신 `application.subscribe` 콜백에서 `state.phase !== 'menu'`로 처음 전이하는 순간을
  감지해 `ownerWindow.setTimeout(..., 0)`으로 한 tick 미룬 뒤 `createPhaserGame()`을 호출한다. 이는
  2차 리뷰가 지목한 근본 원인(같은 동기 콜백 안에서 `hidden` 해제 직후 크기를 재는 문제)을 "나중에
  refresh"가 아니라 "board가 실제로 보인 뒤에 처음부터 올바른 크기로 생성"하는 방식으로 해결한다.
  `game`이 `Phaser.Game | null`로 바뀌었고 `destroy()`의 `game?.destroy(true)`, `relayout: () =>
  game?.scale.refresh()`도 null을 방어적으로 처리한다. `pendingGameMount` 타이머는 unmount 시
  `clearTimeout`으로 정리된다. `game !== null` 가드가 생성 분기를 한 번만 타게 하므로 project 불변
  조건("정확히 하나의 Scene")과 AC07(20 restart에서 subscriber 1개 유지)에 영향이 없다 — 이 로직은
  `ApplicationRouter`/`GameScene` subscribe 경계를 건드리지 않는다.
- 브라우저 재검증 (창 리사이즈 없음): 새 tab에서 fresh preview build를 열고 아무 리사이즈도 하지
  않은 채 Start만 클릭했다.
  - `#board canvas`의 style이 클릭 직후 즉시 `width: 200px; height: 200px`로 관찰되었다(이전
    라운드들은 계속 `0px`였다).
  - canvas는 1개, intrinsic `width="480" height="480"`을 유지했다.
  - 스크린샷으로 20×20 grid, 3칸 snake(head/body 색 구분), food가 모두 정상 렌더링됨을 확인했다.
  - D-pad의 "Move right"를 눌러 `Ready → Playing` 전이와 Pause 버튼 노출을 확인했고, 이후 자동화
    tab이 OS 포커스를 갖지 않아 `document.hidden === true`로 `requestAnimationFrame`이 강하게
    throttle되는 환경 제약을 발견했다(수 초 뒤 snake가 정확히 한 칸 이동한 것으로 확인). 이는 헤드리스
    브라우저 자동화 환경의 특성이며 SG-012 코드의 결함이 아니다 — 실제 tick 진행 로직은 독립적으로
    `fixed-step-scheduler.test.ts`/`application-router.test.ts`(rAF에 의존하지 않는 Node/jsdom
    단위 테스트, 이번 수정으로 변경되지 않은 파일)가 계속 검증한다.
- 비차단 권고: 없음.
- 결론: `SG-012-AC04`를 포함한 모든 수용 기준이 충족된 것으로 판단하며, `verified` 전환을
  권장한다. 원격 push·PR·배포·merge 여부는 Codex integrator의 후속 판단에 맡긴다.
