# 작업 인계: `SG-009`

## 식별 정보

- 담당 AI / 모델 / 설정: Codex / current GPT-5 runtime (assigned Codex 5.6 Terra alias unavailable) / high
- 명세 버전 / task packet revision: `1.0-plan` / revision 1
- 기준 SHA(`base_sha`): `cf811f293918a4b7eed38b5cb44c3673950959cb`
- 브랜치 / 결과 SHA(`head_sha`): `agent/codex/SG-009-pages-artifact-prep` / `331a9545a1634ca5b80594a2fba6e6290b7df25e`
- 연결 Issue / PR: `docs/coordination/tasks/SG-009.yaml` offline 원본; GitHub Issue/PR 없음
- 승인자 / 승인 시각: human-user / `2026-07-19T09:43:59Z`
- 상태: `review-ready`

## 수행 내용

- 변경 목적: PR quality workflow가 repository-derived Pages base로 production build와 preview를 검증한 뒤, 배포 없이 `dist`만 private CI artifact로 올리도록 한다.
- 변경 파일:
  - `.github/workflows/quality.yml`: all prior quality checks 뒤에 `actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4.6.2`를 추가했다. artifact name은 `pages-preview`, path는 정확히 `dist`, 빈 결과는 오류다.
  - `docs/coordination/tasks/SG-009.yaml`: human approval, local-main base, claim, official action tag/SHA research, review evidence를 기록했다.
  - `docs/coordination/handoffs/SG-009.md`: 이 재현 기록.
- 의도적으로 변경하지 않은 범위: `tooling.config.ts`의 shared base normalizer, Vite/Playwright/source/tests/package lock, `dist`, GitHub Pages settings/API, `configure-pages`, `upload-pages-artifact`, `deploy-pages`, environments, concurrency, `gh-pages` branch, remote push와 공개 배포.

## 수용 기준 증거

| AC | 증거(테스트 이름, 스크린샷, 로그 또는 수동 절차) | 결과 |
|---|---|---|
| `SG-009-AC01` | Ruby static-policy check confirms top-level `permissions: contents: read` and rejects `configure-pages`, `upload-pages-artifact`, `deploy-pages`, pages/id-token permissions, environment, concurrency, and `pull_request_target`. GitHub live check returned 0 open PRs/Issues. | PASS |
| `SG-009-AC02` | `GITHUB_REPOSITORY=dragoncowkarma/snake-game ... node tooling.config.ts github-output` produced exactly `base_path=/snake-game/`. Workflow passes that existing shared output to Vite build and preview smoke; it does not hard-code the repository name. Node 24 `npm run build -- --base /snake-game/` passed. | PASS |
| `SG-009-AC03` | Workflow uploads only `dist` with official `upload-artifact` v4.6.2 full SHA. Local artifact inspection found only `dist/index.html`, `dist/assets/index-Df8ZAjDj.css`, and `dist/assets/index-ZXpfZ_1v.js`; `git ls-files` found no tracked dist/report/node_modules artifacts. Full Node 24 `npm run verify` passed before this artifact step can run. | PASS (local/static) |
| `SG-009-AC04` | `PLAYWRIGHT_BASE_PATH=/snake-game/ PLAYWRIGHT_USE_EXISTING_BUILD=1 npm run test:e2e` passed Chromium 1/1 against the existing base-path artifact. The test checks document/asset base containment and records zero page errors, request failures, console errors, or asset HTTP 404 responses. No deploy workflow/action exists. | PASS |

## 실행한 검증

| 명령/환경 | exit code | 실제 결과 |
|---|---:|---|
| Node 24.14.0 clean `npm ci` | 0 | Lockfile 의존성을 clean install. 셸의 Node 22 `npm ci`는 engines warning을 냈으므로 통과로 사용하지 않았고, bundled Node 24 executable로 즉시 재실행했다. |
| Node 24 `npm run format:check` | 0 | Prettier matched files all formatted. |
| Node 24 `npm run lint` | 0 | ESLint `--max-warnings 0` PASS. Sandbox run did not return a terminal exit result, so the same command was rerun outside sandbox and returned 0. |
| Node 24 `npm run typecheck` | 0 | `tsc --noEmit` PASS. |
| Node 24 `npm run test` | 0 | Vitest 5 files / 97 tests PASS. |
| Node 24 `npm run build` | 0 | Vite 8.1.5 production build PASS. |
| Node 24 `npm run verify` | 0 | format → lint → typecheck → Vitest 97/97 → build → Chromium production smoke 1/1 PASS. The test emitted only the environment-level `NO_COLOR`/`FORCE_COLOR` warning; the test's app console/request/page failure assertions passed. |
| Node 24 `npm run build -- --base /snake-game/` | 0 | Repository-prefix artifact build PASS. |
| Node 24 `PLAYWRIGHT_BASE_PATH=/snake-game/ PLAYWRIGHT_USE_EXISTING_BUILD=1 npm run test:e2e` | 0 | Existing artifact Chromium smoke 1/1 PASS. First attempt exited 1 solely because another worktree temporarily held port 4173; its process was confirmed gone without modification, then the same command passed. |
| `GITHUB_REPOSITORY=dragoncowkarma/snake-game ... node tooling.config.ts github-output` | 0 | Exact output: `base_path=/snake-game/`. |
| Ruby YAML and workflow policy checks; `git diff --check`; `git ls-files dist coverage playwright-report test-results node_modules` | 0 | YAML parse/PASS, least-privilege policy/PASS, whitespace/PASS, and no generated artifacts tracked. |
| `git ls-remote` for action tags | 0 | Verified upload-artifact v4.6.2 = `ea165f8d65b6e75b540449e92b4886f43607fa02`; retained checkout/setup-node v7 pins also match their v7.0.0 tags. |

실행하지 못한 검증과 이유:

- GitHub-hosted PR quality run and artifact download were not run: this task does not authorize remote push/PR creation. Local build/preview and workflow static evidence are recorded instead.
- GitHub Pages publish/deploy smoke was intentionally not run: H3a approval is absent and SG-009 has no Pages API/deploy action.

## 결정과 위험

- 새 의존성: 없음.
- 새 결정 또는 기존 결정과의 차이: 없음. Vite's documented `--base` behavior supports the existing normalized repository base. GitHub's current static Pages starter is deployment-oriented; SG-009 deliberately uses the generic artifact action to preserve contents-read-only PR quality. Official sources checked at implementation: <https://vite.dev/guide/build.html>, <https://docs.github.com/en/pages/getting-started-with-github-pages/using-custom-workflows-with-github-pages>, and <https://raw.githubusercontent.com/actions/starter-workflows/main/pages/static.yml>.
- 알려진 위험/제약: CI artifact upload itself awaits a real GitHub PR run. The app has no `release.json`/Pages deploy gate yet; those belong to SG-024. Playwright logs the host environment's `NO_COLOR`/`FORCE_COLOR` warning, but its application-error assertions remain clean.
- 남은 작업과 권장 담당자: Antigravity reviewer should independently inspect the workflow's permissions/action pin/artifact path and reproduce Node 24 `/snake-game/` preview. SG-024 later owns H3a-gated Pages configuration/deploy and rollback runbook; no one should deploy this artifact now.

## 리뷰어 재현 절차

1. `git switch agent/codex/SG-009-pages-artifact-prep` and confirm implementation SHA `331a9545a1634ca5b80594a2fba6e6290b7df25e` plus the packet allowlist.
2. In Node 24, run `npm ci && npm run verify`.
3. Run `npm run build -- --base /snake-game/`, then `PLAYWRIGHT_BASE_PATH=/snake-game/ PLAYWRIGHT_USE_EXISTING_BUILD=1 npm run test:e2e`; inspect that only `dist` is the workflow artifact and no Pages/deploy permission or action exists.
