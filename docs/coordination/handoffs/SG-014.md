# 작업 인계: `SG-014`

## 식별 정보

- 담당 AI / 모델 / 설정: Codex / GPT-5 기반 runtime / exact deployment alias와 setting label 미노출.
- 명세 버전 / task packet revision: `1.0-plan` / revision 1.
- 기준 SHA(`base_sha`): `05cddcd3dbfea9eab6d8319911900580474630b4`.
- 브랜치 / 구현 SHA / 최종 reviewed SHA: `agent/codex/SG-014-vertical-integration` /
  `0b00f65d5258f0b21f69d9e6925289a06ae9ee34` /
  `995e6469171508e7d99a68102f8d76404625a473`.
- 연결 Issue / PR: `docs/coordination/tasks/SG-014.yaml` 단일 offline 원본; 착수 전 GitHub의
  열린 Issue/PR은 각각 0건이었다.
- 승인자 / 승인 시각: human-user / `2026-07-19T15:37:09Z`. 사람의 직접 지시로 exact base에서
  revision 1 packet 작성·승인, SG-012/STATUS 정합화, 전용 브랜치 진행을 승인받았다.
- 상태: `verified`. Claude Sonnet 5 high와 Antigravity Gemini 3.5 Flash high가 모두
  `APPROVE`했고, Codex integrator가 두 review commit과 Antigravity의 한 줄 timeout 보강을 합친
  reviewed SHA `995e6469171508e7d99a68102f8d76404625a473`에서 Node 24 clean verify를 통과시켰다.

## 수행 내용

- 변경 목적: 이미 병합된 SG-010 domain, SG-012 Scene/application adapter, SG-013 DOM shell이
  production entry에서 `DOM command → ApplicationRouter → domain transition → snapshot/event →
  Phaser/UI` 한 방향으로 실제 동작하는지 시작→이동→먹기→벽 사망→재시작 수직 흐름으로 검증한다.
- 변경 파일:
  - `tests/e2e/vertical-flow.spec.ts`: production build에서 실제 Start click과 focused-board
    ArrowRight만 사용해 READY, 한 칸 이동과 음식 1회, Score 10, wall GAME_OVER와 Restart focus,
    zero-score READY 재시작, 단일 visible 480×480 canvas, browser failure 0을 검증한다. 페이지 로드
    전에 브라우저 entropy인 `Math.random`만 고정해 첫 READY food를 `(11,10)`에 두며 제품 test hook은
    추가하지 않는다.
  - `docs/coordination/tasks/SG-014.yaml`: 사람 승인, 정확한 base/revision, claim, allowlist,
    AC, required commands와 review 상태를 기록한다.
  - `docs/coordination/tasks/SG-012.yaml`, `handoffs/SG-012.md`: 독립 3차 APPROVE 뒤 실제 merge
    `05cddcd3dbfea9eab6d8319911900580474630b4`가 완료된 사실을 `verified`에서 `merged`로 정합화한다.
  - `docs/coordination/STATUS.md`: SG-012 merge와 SG-014 진행/검증 상태를 실제 Git 이력과 맞춘다.
  - 이 handoff: AC와 명령의 재현 가능한 증거를 남긴다.
- 의도적으로 변경하지 않은 범위:
  - 제품 코드 전체(`src/domain/**`, `src/config/**`, `src/game/**`, `src/main.ts`, `src/ui/**`, CSS,
    `index.html`)는 변경하지 않았다. 기존 wiring이 accepted 계약을 이미 충족하므로 UI/renderer에
    규칙을 복제하거나 test selector용 제품 로직을 추가하지 않았다.
  - package/lockfile, Vite/Playwright/tooling, workflow, accepted DEVELOPMENT_PLAN/TASKS/CONTRACTS/
    DECISIONS는 변경하지 않았다. 새 의존성도 없다.
  - SG-013의 `src/ui/contracts.ts` type-only re-export shim은 vertical AC에 필요하지 않고 이번
    packet의 금지 경로라 유지했다.
  - `dist`, coverage, Playwright report/result, `node_modules`는 생성됐지만 Git에 추가하지 않았다.

## 수용 기준 증거

| AC | 증거(테스트 이름, 로그 또는 구조) | 결과 |
|---|---|---|
| `SG-014-AC01` | production entry `src/main.ts → mountPhaserGame`; bootstrap의 DOM command가 단일 `ApplicationRouter.dispatch`로 들어가고 router만 domain `reset/enqueueDirection/step`을 호출하며 shell과 `GameScene`은 같은 snapshot/event subscription을 소비한다. SG-014 제품 diff 0. | PASS |
| `SG-014-AC02`, `AC-G02/G05`, `AC-U07` | `plays start, move, eat...`: Start→READY, board focus, intrinsic 480×480 canvas 1개, ArrowRight→PLAYING, `Score 10`, live status `Food eaten. Score 10.` | PASS |
| `SG-014-AC03`, `AC-G07/G10`, `AC-U06/U07` | 동일 E2E가 `Game over: wall collision. Score 10.`, visible result와 Restart focus를 확인하고 click 1회 뒤 READY, Score 0, board focus, canvas 1개를 확인한다. | PASS |
| `SG-014-AC04`, `AC-R03/R05` | test에는 `waitForTimeout`/sleep이 없고 role/accessible name·DOM/canvas 관찰만 사용한다. root와 `/snake-game/` production base 전체 suite가 각각 Chromium 2/2, browser failure 0으로 통과했다. | PASS |
| `SG-014-AC05`, `AC-R04` | Node 24.14.0 clean `npm ci`와 별도 detached worktree `npm run verify`: 121 unit, coverage 97.59% statements/96.61% branches, build, E2E 2/2. whitespace/scope/generated-artifact audit도 0건. | PASS |
| `SG-014-AC06` | SG-010~013 implementation/review SHA와 SG-012 merge `05cddcd` ancestry를 preflight에서 확인했다. SG-012 packet/handoff와 STATUS가 실제 merge에 맞게 갱신됐다. | PASS |

## 실행한 검증

구현 SHA `0b00f65d5258f0b21f69d9e6925289a06ae9ee34`, Codex bundled Node `v24.14.0`,
npm CLI `10.9.3`, 고정 lockfile에서 실행했다.

| 명령/환경 | exit code | 실제 결과 |
|---|---:|---|
| Node 24 `npm ci` / 구현 worktree | 0 | 157 packages clean install, package/lockfile 변경 0. |
| `npm run format:check` | 0 | Prettier 전체 PASS. |
| `npm run lint` | 0 | ESLint `--max-warnings 0` PASS. |
| `npm run typecheck` | 0 | strict project source PASS. |
| `npm run test` | 0 | Vitest 9 files / 121 tests PASS. |
| `npm run test:coverage` | 0 | 121 tests; domain 97.59% statements, 96.61% branches, 100% functions PASS. |
| `npm run build` | 0 | Vite 8.1.5, 24 modules, production build PASS. 기존 Phaser dynamic chunk 500 kB warning만 존재. |
| `npm run test:e2e` | 0 | root production preview, Chromium 2/2 PASS. |
| `PLAYWRIGHT_BASE_PATH=snake-game npm run test:e2e` | 0 | `/snake-game/` production preview, Chromium 2/2 PASS. |
| `npm run verify` / 구현 worktree | 0 | format→lint→typecheck→121 unit→coverage→build→Chromium 2/2 전체 PASS. |
| clean detached checkout `0b00f65…`: Node 24 `npm ci` | 0 | 새 `/tmp` worktree에 157 packages 설치; lockfile diff 0. |
| clean detached checkout `npm run verify` | 0 | 121 unit, 동일 coverage, build, Chromium 2/2 전체 PASS. |
| clean detached checkout Pages-base E2E | 0 | `/snake-game/`, Chromium 2/2 PASS. |
| `git diff --check base..head` | 0 | whitespace 오류 0. |
| allowlist/forbidden/generated scan | 0 | 변경 5파일 모두 allowlist; forbidden product/config/contract diff 0; 추적 생성물 0. |
| SG-014 YAML parse/required-field validator | 0 | id/revision/status/approval/base/scope/AC/commands/claim/handoff 필드 PASS. |

사전 환경 실패와 해소:

- sandbox 안 첫 focused vertical E2E는 localhost `127.0.0.1:4173` bind `EPERM`으로 exit 1이었다.
  같은 구현·Node·명령을 localhost 허용 환경에서 즉시 재실행해 1/1 PASS했고 이후 전체 root/base
  suite와 clean-worktree suite도 모두 통과했다. 앱/테스트 결함이 아니라 sandbox bind 제한이다.
- Playwright child의 기존 `NO_COLOR`/`FORCE_COLOR` 경고와 Vite의 기존 Phaser 500 kB chunk 경고는
  재현되지만 console/page/request 실패나 build 실패가 아니며 숨기거나 임계치를 완화하지 않았다.

실행하지 못한 검증과 이유:

- 지정 Antigravity·Claude 독립 리뷰: 현재 Codex 세션은 다른 플랫폼의 리뷰 승인을 대신할 수 없다.
  구현과 handoff는 review-ready로 제출하며 두 리뷰가 완료되기 전 merge하지 않는다.
- 실기기 320px/다중 입력/20회 restart 독립 audit은 계획상 후속 SG-015 소유다. 이번 SG-014는
  vertical production flow와 기존 20-restart router unit 증거를 통합한다.

## 결정과 위험

- 새 의존성: 없음.
- 새 결정 또는 기존 결정과의 차이: 없음. 제품 코드를 고치지 않고 accepted row-major food 계약의
  index 208을 브라우저 entropy에만 주입해 E2E를 결정론화했다. product hook, query parameter,
  selector 분기, domain/renderer 규칙 복제는 없다.
- 알려진 위험/제약:
  - Phaser dynamic chunk는 minified 1,394.17 kB / gzip 363.54 kB이며 기존 500 kB warning이 남는다.
    DEVELOPMENT_PLAN의 gzip 1MB 목표 안이고 SG-014에서 build 정책을 바꾸지 않았다.
  - SG-013 type-only shim과 실제 storage/audio/pause feature는 각각 후속 정리/SG-016 범위다.
  - 이 브랜치는 독립 리뷰 전 `review`이며 `main`에 병합하거나 push/PR/배포하지 않았다.
- 남은 작업과 권장 담당자:
  1. Antigravity Gemini 3.5 Flash high가 production E2E와 browser failure 수집을 독립 재현한다.
  2. Claude Sonnet 5 high가 one-way wiring과 UI/domain 의미 일치를 검토한다.
  3. 두 리뷰 APPROVE 뒤 Codex integrator가 정확한 reviewed head를 최신 main에 통합하고 packet/
     handoff/STATUS를 `merged`로 동기화한다.

## 리뷰어 재현 절차

1. `agent/codex/SG-014-vertical-integration`의 구현 SHA
   `0b00f65d5258f0b21f69d9e6925289a06ae9ee34`를 clean checkout한다.
2. Node 24에서 `npm ci`, `npm run verify`, `PLAYWRIGHT_BASE_PATH=snake-game npm run test:e2e`,
   `git diff --check 05cddcd3dbfea9eab6d8319911900580474630b4..0b00f65d5258f0b21f69d9e6925289a06ae9ee34`를 실행한다.
3. `tests/e2e/vertical-flow.spec.ts`가 제품 hook 없이 Start/ArrowRight/Restart만 사용하고,
   `waitForTimeout` 없이 UI·canvas·failure listener만 관찰하는지 확인한다.
4. `src/main.ts`, `src/game/bootstrap.ts`, `application-router.ts`, `game-scene.ts`를 따라 DOM command가
   단일 router와 domain을 지나 동일 snapshot/event로 Phaser와 UI에 fan-out하는지 대조한다.
5. base..head diff가 SG-012/STATUS 정합화, SG-014 packet, vertical E2E 다섯 파일뿐이고
   domain/UI/config/package/accepted 계약 diff와 추적 생성물이 0인지 확인한다.

## 독립 리뷰 — Claude Sonnet 5

- 판정: **APPROVE — 차단 결함 0건.** 지정된 두 리뷰 중 Claude 쪽을 종료하며, Antigravity Gemini
  3.5 Flash high의 독립 리뷰가 완료되기 전에는 `verified`/`merged`로 전환하지 않는다.
- 검토 대상: 구현 SHA `0b00f65d5258f0b21f69d9e6925289a06ae9ee34`, 문서 커밋
  `f92ced6c81ccf09813cd03bff6f055974d8a3d28` (기존 `claude/sg-014-review-b643af` worktree HEAD가
  이미 이 커밋).
- 범위 대조: `git diff --stat 05cddcd..f92ced6`이 `STATUS.md`, `SG-012.md`, `SG-012.yaml`,
  `SG-014.md`, `SG-014.yaml`, `vertical-flow.spec.ts` 여섯 파일만 바꿨음을 확인했다. allowlist를
  벗어난 파일이나 `src/domain/**`·`src/config/**`·`src/ui/**`·package/lockfile·vite/playwright/
  tooling config·`.github/**`·accepted 계약 문서에 대한 diff는 0건이다. `git diff --check`도
  whitespace 오류 0건을 확인했다.
- 독립 환경: 기존 worktree에서 Node 22.18.0으로 실행했다(`package.json`/`.nvmrc`는 24.x를
  요구하며 `npm ci`가 `EBADENGINE` 경고를 남겼지만 157 packages clean install, lockfile diff
  0으로 계속 진행했다). `npm run verify`(format:check → lint → typecheck → 121/121 unit →
  coverage 97.59% statements / 96.61% branches / 100% functions → build → Chromium E2E 2/2)와
  `PLAYWRIGHT_BASE_PATH=snake-game npm run test:e2e`(Chromium 2/2)를 재실행했고 전부 exit 0으로
  handoff 기록과 일치했다.
- 결정론 검증: handoff의 주장을 그대로 믿지 않고 직접 계산했다. `src/config/game-config.ts`의
  20×20 board와 초기 snake `(10,10)/(9,10)/(8,10)`, `src/domain/food-spawner.ts`의 y-major/
  x-minor free-cell 스캔을 손으로 추적하면 `y=0..9`가 index 0-199, `y=10`의 `x=0..7`이 index
  200-207, occupied `x=8,9,10`을 건너뛰고 `x=11`이 index 208이 된다 — handoff가 주장한
  `(11,10)`과 정확히 일치한다.
- 배선 검토: `src/main.ts → bootstrap.mountPhaserGame → ApplicationRouter.dispatch`가 유일한
  domain 진입점이고(`reset`/`enqueueDirection`/`step`만 호출), `GameScene`과 DOM shell 둘 다
  같은 `application.subscribe` 스트림만 구독해 렌더링하며 별도 게임 규칙 분기가 없음을 코드
  리딩으로 확인했다. `InputController`도 board keydown과 버튼 클릭이 동일한 `dispatch` closure로
  수렴하도록 유지되어 있다 — AC01의 one-way wiring 주장과 일치하며 이번 packet에서 이 파일들은
  변경되지 않았다.
- SG-012 정합화 검증: 병합 커밋 `05cddcd`의 두 번째 parent(`c64c936`)가 실제 리뷰 대상이었던
  SG-012 head `3393cb7`를 조상으로 포함하고, 둘 다 `main`의 조상임을 `git merge-base
  --is-ancestor`로 확인했다 — STATUS/SG-012 handoff/yaml의 `merged` 정합화 서술이 실제 이력과
  일치한다.
- 비차단 권고: 로컬 검증을 pinned Node 24 대신 22.18.0으로 수행했다(EBADENGINE 경고만 있었고
  기능적 실패는 없음). 병합 전 어느 시점에 실제 Node 24 런타임에서 한 번 더 확인할 것을 권장하나
  이 리뷰의 APPROVE 판정을 막는 결함은 아니다.
- 다음 단계: Antigravity Gemini 3.5 Flash high의 production E2E/browser-failure 독립 재현이
  남았다. 두 리뷰 모두 APPROVE된 뒤 Codex integrator가 최신 `main` 위에 통합하고 packet/handoff/
  STATUS를 `merged`로 동기화한다.

## 독립 리뷰 — Antigravity

- **판정: APPROVE — 차단 결함 0건, `verified` 권장.**
- **검토 대상:** 구현 SHA `0b00f65d5258f0b21f69d9e6925289a06ae9ee34` 및 `tests/e2e/vertical-flow.spec.ts` 수정 사항.
- **검증 환경:** Node `v22.18.0`, npm `10.9.3`, macOS.
- **수행 검증:** `npm ci` 후 `npm run verify` 및 `PLAYWRIGHT_BASE_PATH=snake-game npm run test:e2e` 모두 `PASS`를 확인. `git diff --check`로 whitespace 오류가 없음을 확인.
- **수정/보강 사항:**
  - 헤드리스 브라우저(Playwright) 자동화 환경에서 `requestAnimationFrame`이 OS 백그라운드 스케줄링으로 인해 스로틀링(throttling)되어 게임 step이 느려질 수 있습니다.
  - 이로 인해 snake가 9칸을 이동해 벽에 충돌할 때까지 걸리는 시간이 10초를 초과하여 `expect(phase).toHaveText('Game over')` 단계에서 타임아웃 오류가 발생하였습니다.
  - 이에 따라 해당 단일 `expect` 단계를 `expect(phase).toHaveText('Game over', { timeout: 30000 })`로 보강하였으며, 변경 후 모든 E2E 테스트가 20초대에 안정적으로 통과(`PASS`)함을 확인하였습니다.

## 통합 책임자 검증 — 두 리뷰 합성

- 판정: **APPROVE — SG-014 `verified`, main 통합 준비 완료.**
- 최종 reviewed SHA: `995e6469171508e7d99a68102f8d76404625a473`. Claude review
  `f520264ac45249b229f6337d7803e27195e46865`와 Antigravity review/remediation
  `e3dee624b5c1630d89b3f237a338d68e6fd9f606`이 모두 조상이다.
- 테스트 변경 검토: executable diff는 `Game over` phase assertion의 최대 timeout을 10초에서
  30초로 늘린 한 줄뿐이다. fixed sleep/`waitForTimeout`, 제품 hook, selector 분기, production/config
  변경은 없고 정상 환경에서는 phase 전환 즉시 반환한다.
- Node 24.14.0 현재 worktree: `npm run verify` exit 0 — 121/121 unit, coverage 97.59%
  statements/96.61% branches/100% functions, build, Chromium 2/2. `/snake-game/` Chromium 2/2.
- 동일 SHA 별도 detached clean checkout: `npm ci`, 전체 `npm run verify`, `/snake-game/` E2E가 모두
  exit 0. scope/whitespace/YAML/review ancestry/generated artifact 검사도 PASS했다.
- 리뷰 문서의 `reviewed_at`은 reviewer commit의 실제 UTC 시각으로 정정했다. 판정·검증 내용은
  변경하지 않았다.
