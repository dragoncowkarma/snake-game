# 작업 인계: `SG-013`

## 식별 정보

- 담당 AI / 모델 / 설정: Claude / Sonnet 5 / 높음
- 명세 버전 / task packet revision: `1.0-plan` / revision 1
- 기준 SHA(`base_sha`): `cf811f293918a4b7eed38b5cb44c3673950959cb`
- 브랜치 / 구현 SHA(`head_sha`) / 검증 기록 SHA: `claude/sg-013-ui-implementation-bd0257` / `3a443185e8f4775fc27032f6ae6638abf8fb3fc7` / `664cd480739a671d7cc371f964691fe3391f3972`
- 통합 기준 SHA / 통합 커밋: `cf811f293918a4b7eed38b5cb44c3673950959cb` / `8391960c9fd01315db55053bf7c1d12459f6c702`
- 연결 Issue / PR: `docs/coordination/tasks/SG-013.yaml` offline packet; GitHub Issue/PR 없음
- 승인자 / 승인 시각: human-user(세션 내 직접 채팅 지시, `AskUserQuestion`) / 2026-07-19(정확한 UTC 시각은 환경에서 확인 불가)
- 상태: `merged` — local integration commit `8391960c9fd01315db55053bf7c1d12459f6c702`; 원격 push·배포 없음

## 시작 전 확인 사항과 세션 내 승인 경위

작업 시작 전 `AGENTS.md`, `docs/DEVELOPMENT_PLAN.md`, `docs/TASKS.md`의 SG-013 행,
`docs/coordination/STATUS.md`, `docs/coordination/DECISIONS.md`, 그리고 SG-006의
packet/handoff를 순서대로 읽었다. `STATUS.md`는 다음을 명시했다.

> 현재 결정 필요: SG-013 packet 작성 전 SG-006에서 선행 구현한 focus/aria-live/
> Command-routing 범위를 `TASKS.md`와 `DECISIONS.md`에 정합화
>
> SG-013은 SG-006과의 범위 정합화가 끝나기 전 packet화하지 않는다.

`docs/coordination/tasks/SG-013.yaml`/`handoffs/SG-013.md`는 존재하지 않았고, 이
프롬프트의 승인 필드(`base_sha`, `approved_by`, `approved_at`, `allowed_paths`,
`forbidden_paths`, `acceptance_ids`, `required_commands`)도 모두 미채움
placeholder였다. `AGENTS.md` 3절은 승인 필드가 없으면 시작하지 말라고 규정하고,
`STATUS.md`는 이 특정 작업(SG-013)을 명시적으로 보류시켰다. 이에 따라 코드를 전혀
건드리지 않고 `AskUserQuestion`으로 사람에게 직접 물었다.

세 선택지를 제시했다.

1. **축소 범위로 직접 승인**(선택됨) — SG-006이 이미 사람 승인으로 구현한
   DOM shell/focus 전이/aria-live/Command 라우팅을 TASKS.md 원안의 SG-013 몫으로
   인정하고, 이번 SG-013 범위를 "실제 `src/domain`(SG-007/SG-010, merged) 연동 +
   hand-authored fixture 대신 실제 도메인 산출물 기반 종단 증거"로 재정의해 바로
   진행.
2. 문서 정합화만 먼저(코드 변경 없음).
3. 중단하고 조정 책임자(Codex) 대기.

사람이 1번을 선택했다. 이 선택은 `docs/coordination/tasks/SG-013.yaml`의
`approval_basis`/`scope_boundary`/`reconciliation_note`에 근거와 함께 기록했다.
이 승인은 또한 SG-006의 `tests/e2e/scaffold.spec.ts` 일회성 예외와 같은 방식으로,
`docs/TASKS.md`의 SG-013 행과 `docs/coordination/STATUS.md`(둘 다 통상적인 SG-013
구현 packet의 허용 경로 밖)를 이번 packet에서 함께 수정하도록 허용했다 — STATUS.md
자신이 이 정합화를 차단 조건으로 지목했으므로 그 두 파일을 건드리지 않고는 조건을
해소할 수 없었기 때문이다. `docs/coordination/DECISIONS.md`는 의도적으로 건드리지
않았다(§결정과 위험).

## 수행 내용

- 변경 목적: (1) `src/ui/contracts.ts`의 수동 유지 타입 미러를 실제
  `src/domain/index.ts`(SG-007/SG-010, merged) type-only re-export로 교체해 정의
  드리프트 위험을 제거하고, (2) hand-authored fixture 대신 실제 `reset`/
  `enqueueDirection`/`step` 산출물로 SG-006 shell의 focus-전이/aria-live 종단
  증거를 보강해 SG-006 handoff가 명시적으로 남긴 공백("실제 게임플레이로 focus/
  aria-live/Command 라우팅을 구동한 종단 증거")을 닫는다.
- 변경 파일:
  - `src/ui/contracts.ts`: 내용을 전면 교체. 기존에는 `docs/coordination/
    CONTRACTS.md` §1을 손으로 다시 타이핑한 타입 선언이었으나, 이제는
    `export type { ... } from '../domain/index.ts';` 한 블록으로 대체했다. 이
    파일 자체는 지우지 않았다 — `tests/fixtures/fixtures.ts`, `tests/fixtures.test.ts`,
    `tests/helpers/ev-fail-01.ts`(Antigravity 소유, 이 packet의 허용 경로 밖)가
    정확히 이 경로(`src/ui/contracts.ts`)를 import하므로, 파일을 지우고 각
    import 지점을 `../domain/index.ts`로 바꾸는 편이 더 넓은 범위(다른 AI 소유
    파일 수정)가 되어 대신 재-export shim으로 남겼다. `src/ui/app.ts`,
    `src/ui/state.ts`, `src/ui/render.ts`, `src/ui/main.ts`, `src/ui/
    state.test.ts`는 여전히 `./contracts.ts`에서 import하므로 이 파일들 자체는
    한 글자도 바꾸지 않았다.
  - `src/ui/state.domain-integration.test.ts`(신규): 실제 `reset`/
    `enqueueDirection`/`step`을 고정 인덱스 `RandomSource`(파일 내부 지역
    클래스, 새 의존성 아님)로 구동해 (a) ready→playing 시작,
    (b) 이동만 있는 tick, (c) 실제 food-spawn 산출물을 먹는 tick,
    (d) 실제 벽 충돌로 `gameOver`에 도달하는 11-tick 시퀀스와 그 이후 terminal
    재-step의 이벤트 미발생을 만들고, `state.ts`의 `focusTargetForTransition`/
    `announcementFor`가 이 실제 산출물에 대해 올바른 focus 대상과 안내문을
    반환하는지 검증한다. food 위치(`{x:11,y:10}`)는 `docs/coordination/
    CONTRACTS.md` §6의 row-major free-cell 열거를 근거로 계산한 인덱스(208)로
    결정론적으로 얻었으며(주석에 산출 근거 기록), 추측이 아니다.
  - `docs/TASKS.md`: SG-013 행의 "작업과 산출물" 셀만 수정해 SG-006 선구현
    사실과 이번 SG-013의 실제(축소) 범위를 기록. 다른 셀·다른 행은 변경 없음.
  - `docs/coordination/STATUS.md`: (1) "현재 검증 대기"/"현재 결정 필요" 갱신,
    (2) SG-006 기록 다음에 SG-013 기록 추가, (3) "다음 관문" 문단의 SG-013
    문장 갱신, (4) Wave 스냅샷 표의 Wave 2 비고 갱신. 다른 문단·다른 작업 기록은
    변경 없음.
  - `docs/coordination/tasks/SG-013.yaml`, `docs/coordination/handoffs/SG-013.md`:
    이번 packet과 handoff.
- 의도적으로 변경하지 않은 범위: 게임 규칙, Phaser Scene(`src/game/**`는 아직
  없음, SG-012 대상), `src/domain/**`(SG-007/SG-010 소유, import만 함),
  `src/config/**`, application command router(§9 CONTRACTS.md가 domain과
  input adapter 사이에 두는 개념 — `src/main.ts`/향후 `src/game/**` 소관이며
  `src/ui/main.ts`의 `dispatchStub`은 SG-006 상태 그대로 콘솔 로그만 남기는
  스텁으로 유지), `src/ui/app.ts`/`state.ts`/`render.ts`의 동작 로직(오직 타입
  import 출처만 바뀔 뻔했으나 실제로는 그마저도 바뀌지 않음 — 위 참고),
  `src/ui/state.test.ts`(SG-006이 만든 hand-fixture 테스트는 그대로 유지 —
  fixture 기반 증거와 실제 domain 기반 증거는 상호 보완적이므로 대체가 아니라
  추가), `docs/coordination/DECISIONS.md`, 다른 모든 task packet/handoff, 실제
  오디오/저장소 구현.

## 수용 기준 증거

| AC | 증거(테스트 이름, 스크린샷, 로그 또는 수동 절차) | 결과 |
|---|---|---|
| `SG-013-AC01` / `AC-U06`, `AC-U07` | `state.domain-integration.test.ts` 4개 케이스(§실행한 검증) — 실제 `reset`/`enqueueDirection`/`step` 산출물에 대해 `focusTargetForTransition`/`announcementFor`가 계약대로 동작 | PASS |
| `SG-013-AC02` / `AC-U06`, `AC-U07` | 실제 Chromium(Claude Browser, dev server, jsdom 아님)에서 `createGameShell` 인스턴스를 실제 `reset(ready)`→`enqueueDirection(right)`→`step`(food 획득)→(별도 시퀀스로) `enqueueDirection(up)`→11×`step`(벽 충돌) 순으로 구동. 실측: ready 진입 후 `document.activeElement.id === 'board'`이고 `#status`가 "Ready. Press a direction to start."; start 후 `#status`가 "Playing."; food 획득 tick 후 `#status`가 "Food eaten. Score 10." 그리고 HUD `Score 10`; gameOver 진입 후 `document.activeElement`가 Restart 버튼(class `action action--primary`, text `Restart`)이고 `#status`가 "Game over: wall collision. Score 0.", `.hud__result`가 "Game over (wall collision). Score 0." (§리뷰어 재현 절차). 전 과정 콘솔 오류 0건 | PASS |
| `SG-013-AC03` / CONTRACTS.md §9 타입 경계 | `src/ui/contracts.ts`가 `export type { ... } from '../domain/index.ts';`만 포함(런타임 로직·중복 선언 없음). `npm run typecheck` 0 — `tests/fixtures/fixtures.ts`, `tests/fixtures.test.ts`, `tests/helpers/ev-fail-01.ts`가 이 경로로 그대로 컴파일됨을 함께 확인 | PASS |

SG-006이 이미 닫은 AC-U01~U05/U08~U09(레이아웃, 44px, 단일 dispatch, 대비,
safe-area, reduced-motion)는 이번 packet에서 관련 코드를 변경하지 않았으므로
재검증하지 않았다 — `docs/coordination/handoffs/SG-006.md`의 기록이 여전히
유효하다.

## 실행한 검증

| 명령/환경 | exit code | 실제 결과 |
|---|---:|---|
| `npm run format:check` | 0 | Prettier 전체 PASS |
| `npm run lint` | 0 | ESLint `--max-warnings 0` PASS |
| `npm run typecheck` | 0 | strict TypeScript PASS(신규 `state.domain-integration.test.ts` 포함) |
| `npm run test` | 0 | Vitest 6 files / **101 tests** PASS(기존 97 + 신규 4) |
| `npm run build` | 0 | Vite 8.1.5; JS 8.13 kB(gzip 2.96 kB), CSS 3.91 kB(gzip 1.27 kB), HTML 0.55 kB — SG-006 대비 번들 크기 불변(동작 로직 변경 없음) |
| `npm run test:e2e`(Chromium, `tests/e2e/scaffold.spec.ts`, 무변경) | 0 | 1/1 PASS — console/page/request/HTTP failure 0 |
| `npm run verify`(전체 체인 재확인) | 0 | 위 6개 명령 순서대로 모두 PASS |
| `git diff --check` | 0 | whitespace 오류 0 |
| Claude Browser(Chromium, `npm run dev` 수동 기동, 320×568) 실제 도메인 구동 | N/A | §수용 기준 증거 SG-013-AC02, §리뷰어 재현 절차. 콘솔 오류 0(`read_console_messages` `onlyErrors: true` → 로그 0) |

추가 검증과 남은 종단 증거:

- Antigravity 독립 리뷰: 검증 기록 `664cd480739a671d7cc371f964691fe3391f3972`에서
  format, lint, typecheck, test, build, E2E 전체 suite를 재실행하고 DOM shell의 실제
  domain type re-export 및 state 경계를 검토해 통합을 승인했다. 저장소 기록에는 원시
  로그나 SG-013-AC02 수동 브라우저 절차의 독립 재실행 주장은 없으므로, 해당 수동 증거는
  Claude 작성자 증거로 유지한다.
- Codex 통합 재검증: 검증 브랜치를 local `main`
  `cf811f293918a4b7eed38b5cb44c3673950959cb` 위에 충돌 없이 merge commit
  `8391960c9fd01315db55053bf7c1d12459f6c702`으로 통합했다. bundled Node
  `v24.14.0`에서 format, lint, typecheck, 6 files / 101 tests, build를 통과했다.
  Chromium E2E는 sandbox localhost bind `EPERM` 뒤 권한 환경에서 재실행했지만,
  configured 4173 포트가 clean SG-012 review preview에 점유돼 있었다. 그 프로세스를
  건드리지 않고 `tooling.config.ts`의 포트만 uncommitted 4174로 일시 치환해 1/1 PASS한
  뒤 즉시 원복했으며 최종 diff는 0이다. scope와 `git diff --check`도 통과했다.
- 실제 스크린리더(VoiceOver/TalkBack)·실기기·크로스브라우저 증거: SG-018/
  SG-020(Antigravity) 배정 그대로이며 이번 packet도 이를 넘겨받지 않는다.
- SG-012(Phaser Scene)/SG-014(전체 통합) 전까지는 실제 렌더 루프·실제 사용자
  키 입력이 실제 command router를 거쳐 domain을 구동하는 완전한 종단 증거는
  존재할 수 없다(command router 자체가 아직 없음). 이번 packet이 닫은 것은
  "domain이 만드는 실제 GameState/DomainEvent 모양에 대해 shell이 올바르게
  반응하는가"이지, "실제 키 입력이 실제 domain을 실제로 구동하는가"가 아니다 —
  후자는 SG-014의 범위다.

## 결정과 위험

- 새 의존성: 없음. `package.json`은 이 packet의 허용 경로 밖이라 수정하지
  않았다. `state.domain-integration.test.ts`의 `FixedIndexRandomSource`는
  파일 내부 지역 클래스이며 새 devDependency(jsdom 등)를 추가하지 않았다 —
  이 때문에 DOM 수준 실제-도메인 구동 증거(SG-013-AC02)는 자동화된 단위
  테스트가 아니라 §리뷰어 재현 절차의 수동 브라우저 절차로 남겼다. jsdom
  추가는 `package.json` 변경이 필요해 이 packet 범위 밖이다.
- 새 결정 또는 기존 결정과의 차이:
  - `docs/TASKS.md`/`docs/coordination/STATUS.md`를 이번 packet에서 함께
    수정한 것은 정상적인 SG-013 허용 경로를 벗어난 일회성 예외이며, 그 근거는
    `docs/coordination/tasks/SG-013.yaml`의 `approval_basis`/
    `allowed_paths_note`에 기록했다.
  - `docs/coordination/DECISIONS.md`는 의도적으로 수정하지 않았다. 이번
    reconciliation은 새 제품 의미·공개 계약·ADR을 만들지 않으므로(SG-003
    revision 2가 §10에서 쓴 것과 같은 논리), 새 `D-00N` 항목 대신
    `STATUS.md`의 운영 기록으로 처리했다. 조정 책임자가 다른 판단을 하면
    후속 packet에서 `DECISIONS.md`에 반영할 수 있다.
  - `src/ui/contracts.ts`를 삭제하지 않고 re-export shim으로 유지하기로 한
    것은 SG-006 handoff가 "SG-007 완료 뒤 삭제하고 실제 모듈을 import"라고
    권고한 것과 문자 그대로는 다르다(삭제 대신 shim화). 이유는 §변경 파일에
    적었듯 `tests/**`(Antigravity 소유) 세 파일이 이 경로에 의존하기
    때문이며, 파일을 지우고 그 세 파일의 import 문을 고치는 것은 SG-013의
    허용 경로 밖 수정이 된다. Shim화는 SG-006 권고의 핵심 목적(타입 드리프트
    제거)은 달성하면서 경로 소유권을 넘지 않는다.
- 알려진 위험/제약:
  - `src/ui/contracts.ts`가 여전히 존재한다. 최종 삭제에는 Antigravity 소유의
    `tests/fixtures/fixtures.ts`, `tests/fixtures.test.ts`,
    `tests/helpers/ev-fail-01.ts`뿐 아니라 Claude 소유의 `src/ui/app.ts`, `main.ts`,
    `render.ts`, `state.ts`, `state.test.ts`도 실제 `src/domain/index.ts`를 직접 import하도록
    바꿔야 한다. 따라서 tests-only 정리로는 shim을 삭제할 수 없으며, cross-owner 정리
    packet 또는 SG-014 통합 범위에서 처리해야 한다.
  - `src/ui/main.ts`의 `dispatchStub`은 여전히 프로덕션에서 명령을 어디에도
    연결하지 않는다 — SG-012/SG-014 전까지는 의도된 상태다.
  - `src/main.ts`/`src/main.test.ts` orphan 파일이 SG-006 때부터 그대로
    남아 있다(§SG-006 handoff에 이미 기록됨, 이번 packet 범위 밖).
- 남은 작업과 권장 담당자:
  - `src/ui/contracts.ts` shim의 최종 삭제와 위 8개 consumer import 정리 —
    Antigravity + Claude cross-owner cleanup 또는 SG-014 통합 담당.
  - 실제 command router 구현과 `src/ui/main.ts`의 `dispatchStub` 교체 —
    SG-012(Phaser Scene)/SG-014(통합), Codex.
  - 실기기/실 AT/크로스브라우저 종단 증거 — SG-018/SG-020, Antigravity.

## 리뷰어 재현 절차

1. `git status`/`git diff docs coordination`로 변경 파일이 `docs/coordination/
   tasks/SG-013.yaml`의 `allowed_paths`(+ 명시된 일회성 예외 두 파일) 안인지
   대조한다.
2. Node 24에서 `npm run verify`를 실행해 모든 하위 명령이 exit 0인지 확인한다.
3. `npm run dev -- --port 5174`(또는 임의 포트)로 개발 서버를 띄우고,
   브라우저에서 `http://localhost:5174/`를 320×568로 연 뒤 콘솔에서 아래를
   실행해 실제 domain이 구동하는 shell 반응을 재현한다.

   ```js
   const domain = await import('/src/domain/index.ts');
   const appMod = await import('/src/ui/app.ts');
   class FixedIndexRNG {
     constructor(i) { this.i = i; }
     nextInt(n) { if (this.i >= n) throw new Error('bad'); return this.i; }
   }
   const root = document.querySelector('#app');
   root.replaceChildren();
   const shell = appMod.createGameShell(root, () => {});

   const rng = new FixedIndexRNG(208); // (11,10): CONTRACTS.md §6 row-major math
   const ready = domain.reset({ difficulty: 'normal', phase: 'ready' }, rng);
   shell.applySnapshot(ready.state, ready.events);
   // document.activeElement.id === 'board'; #status === 'Ready. Press a direction to start.'

   const start = domain.enqueueDirection(ready.state, 'right');
   shell.applySnapshot(start.state, []);
   // #status === 'Playing.'

   const tick = domain.step(start.state, rng);
   shell.applySnapshot(tick.state, tick.events);
   // tick.events[0].type === 'foodEaten'; #status === 'Food eaten. Score 10.'
   ```

4. 벽 충돌 재현: 같은 페이지에서 `FixedIndexRNG(0)`으로 새 `reset`, `up`으로
   `enqueueDirection`, `step`을 11회 반복 호출한 뒤 마지막 결과를
   `shell.applySnapshot`에 넣으면 `document.activeElement`가 Restart 버튼이고
   `#status`가 `"Game over: wall collision. Score 0."`임을 확인할 수 있다
   (정확한 스니펫은 `src/ui/state.domain-integration.test.ts`의 동일 시나리오
   참고).
5. `git diff --name-only cf811f293918a4b7eed38b5cb44c3673950959cb..HEAD`로
   변경 파일 목록이 이 handoff의 §변경 파일과 정확히 일치하는지 확인한다.
