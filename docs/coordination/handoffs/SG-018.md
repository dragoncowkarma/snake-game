# 작업 인계: `SG-018`

## 식별 정보

- 담당 AI / 모델 / 설정: Antigravity / Gemini 3.5 Flash high / high
- 명세 버전 / task packet revision: 1.0-plan / revision 1
- 기준 SHA(`base_sha`): `43dc22c66b552e6e6b05fc61ad58c591ffac023d`
- 브랜치 / 결과 SHA(`head_sha`): `agent/antigravity/SG-018-e2e-testing` / `30dcfa5da493714895ca52713b5bf81bded927f1`
- 연결 Issue / PR: `docs/coordination/tasks/SG-018.yaml` 단일 offline 원본
- 승인자 / 승인 시각: human-user / `2026-07-21T01:15:50Z`
- 상태: `review-ready` (테스트 자체는 Codex 리뷰 전까지 PASS로 판정하지 말라는 규칙에 따라 `review-ready` 상태로 인계합니다. 단, 테스트 설계와 검증 자체는 통과했습니다.)

## 수행 내용

- 변경 목적: production dist 빌드 결과물을 대상으로 Playwright를 사용해 키보드/포커스/1:1 터치/일시정지/창 크기 조정/로컬 저장소/재시작/콘솔 로그 수집/404 자산 등을 완벽히 검증하는 안정적인 E2E 테스트 스위트를 작성하고, `waitForTimeout`에 대한 의존성 없이 안정적으로 3회 연속 통과시킵니다.
- 변경 파일:
  - `tests/e2e/production.spec.ts`: production dist 빌드를 대상으로 하는 Playwright E2E 테스트 스위트 추가
  - `docs/coordination/tasks/SG-018.yaml`: 태스크 선점 및 상태 정보 기록
  - `docs/coordination/STATUS.md`: 활성 태스크 상태 수정
- 의도적으로 변경하지 않은 범위:
  - `src/**` 아래의 모든 제품 코드 (테스트 엔지니어 권한으로 제품 코드를 수정하지 않음)
  - `package.json`, `package-lock.json`, `vite.config.ts`, `playwright.config.ts` 등의 프로젝트 환경 파일
  - `tests/unit/**` 및 기타 기존 E2E 테스트 스펙 파일

## 수용 기준 증거

| AC | 증거(테스트 이름, 스크린샷, 로그 또는 수동 절차) | 결과 |
|---|---|---|
| SG-018-AC01 | `Keyboard Controls and Focus Transitions` - Arrow/WASD 키 입력 맵핑 검증, handled game keys에 대해 preventDefault 작동을 통한 스크롤 방지 동작 확인 | PASS |
| SG-018-AC02 | `Keyboard Controls and Focus Transitions` - Start->board, Pause->Resume, GameOver->Restart, Menu->Start 포커스 이동 전이 검증 | PASS |
| SG-018-AC03 | `Touch D-pad controls` - `.dpad__button--*` 버튼 클릭 시 방향 지시가 발생하며 포커스가 튀지 않고 원활히 진행됨 확인 | PASS |
| SG-018-AC04 | `Lifecycle Pause & Resize` - `visibilitychange` (document.hidden = true), window `blur` 및 `orientationchange` 발생 시 Paused 상태로 자동 전환 확인 | PASS |
| SG-018-AC05 | `Lifecycle Pause & Resize` - 브라우저 크기 조정(`setViewportSize` 600x600)을 가해도 게임이 일시정지되지 않고 `Playing` 상태와 점수를 보존함 확인 | PASS |
| SG-018-AC06 | `LocalStorage persistence` - 게임 종료 후 localStorage에 `normal` 난이도의 고점(10) 및 음소거 설정(`true`)이 정상 영속화되고, 새로고침 시 로드됨 확인 | PASS |
| SG-018-AC07 | `LocalStorage failure fallback` - localStorage가 `SecurityError` (DOMException)를 던지도록 가정한 경우에도 게임이 아무런 예외 크래시 없이 정상 플레이 가능함 확인 | PASS |
| SG-018-AC08 | `Audio fallback when AudioContext fails` - `window.AudioContext` 및 `webkitAudioContext`가 없는 환경(또는 초기화 실패)에서도 크래시 없이 폴백 작동 확인 | PASS |
| SG-018-AC09 | 모든 테스트에 적용된 `setupPageListeners`가 런타임 중 console error, pageerror, 404 http status 등을 감지하여 에러 목록이 비어 있음을 단언 | PASS |

## 실행한 검증

Node `v24.13.3` 및 npm `11.18.0` 환경에서 수행되었습니다.

| 명령/환경 | exit code | 실제 결과 |
|---|---:|---|
| `npm run format:check` | 0 | Prettier 포맷 오류 없음을 확인 |
| `npm run lint` | 0 | ESLint 경고 없음을 확인 |
| `npm run typecheck` | 0 | TypeScript strict check 성공 |
| `npx vitest run --exclude "tests/e2e/**" --exclude "spikes/**" --exclude ".claude/worktrees/**" --exclude ".codex/worktrees/**" --exclude ".gemini/**"` | 0 | 12개 테스트 파일, 128개 테스트 전체 PASS (nested worktree 오염 제외) |
| `npm run build` | 0 | Vite build 성공 (기존 Phaser 500kB warning 외 오류 없음) |
| `npm run test:e2e` (1회차) | 0 | 14개 테스트 전체 PASS (1.7m) |
| `npm run test:e2e` (2회차) | 0 | 14개 테스트 전체 PASS (1.7m) |
| `npm run test:e2e` (3회차) | 0 | 14개 테스트 전체 PASS (1.7m) |
| `git diff --check` | 0 | whitespace 오류 없음 |

실행하지 못한 검증과 이유:
- `npm run test` 및 `npm run verify` 스크립트 원문 실행: `STATUS.md` 및 이전 세션의 handoff에 기록된 바와 같이, `package.json`의 Vitest spikes 제외 패턴 결함으로 인해 nested worktrees 안의 spikes spec 파일들을 vitest가 로드하려다 RolldownError를 던지는 사전 결함 때문입니다. 이는 태스크 허용 경로 밖의 수정이 필요하므로, 대신 spikes를 포함한 nested worktrees를 명시적으로 제외한 scoped vitest 명령으로 unit tests 128개를 검증했습니다.

## 결정과 위험

- 새 의존성: 없음
- 새 결정 또는 기존 결정과의 차이: 없음. `DEVELOPMENT_PLAN` 및 `CONTRACTS.md` 상에 명시된 규칙(예: 보드 밖 포커스 시 단축키가 browser default를 방해하지 않는 캡처 차단 등)의 동작 방식을 E2E 상에 정확히 녹여내어 설계하였습니다.
- 알려진 위험/제약:
  - `orientationchange` E2E 테스트를 위해 `window.screen.orientation`을 Mocking하는 스크립트를 페이지 내에 삽입하여 수행합니다. 이는 실제 기기 방향 전환 시 `LifecycleController`가 이전 orientation값과 달라진 경우에만 pause를 호출하는 제약을 극복하고 Headless 브라우저 상에서 안정적인 E2E를 확보하기 위함입니다.
- 남은 작업과 권장 담당자:
  - Codex 독립 리뷰어가 작성된 E2E 시나리오(`tests/e2e/production.spec.ts`)를 최종 검토 및 APPROVE하고 `merged`를 진행합니다.

## 리뷰어 재현 절차

1. `agent/antigravity/SG-018-e2e-testing` 브랜치를 체크아웃합니다.
2. `npm ci`를 통해 의존성을 설치하고, `npm run build`를 수행해 dist 빌드가 정상인지 확인합니다.
3. `npm run test:e2e`를 실행하여 14개 테스트(기존 audit + 새 production specs)가 모두 PASS하는지 확인합니다.
4. `tests/e2e/production.spec.ts` 파일을 검토하여 `waitForTimeout`을 쓰지 않고 locator와 async page triggers로 동작하는지 확인합니다.
