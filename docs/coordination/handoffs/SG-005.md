# 작업 인계: `SG-005`

## 식별 정보

- 담당 AI / 모델 / 설정: Codex / 현재 GPT-5 runtime(정확한 5.6 Terra 별칭 미노출) / high
- 명세 버전 / task packet revision: `1.0-plan` / revision 1
- 기준 SHA(`base_sha`): `db5711edce7c6eb8a077b18216857fcf47226df6`
- 브랜치 / 결과 SHA(`head_sha`): `agent/codex/SG-005-scaffold` / `15f6b5ae9e8c5bb52f8027d78e7b727008042ac7`
- 연결 Issue / PR: `docs/coordination/tasks/SG-005.yaml` offline 원본; GitHub Issue/PR 없음
- 승인자 / 승인 시각: Codex integrator / `2026-07-18T09:40:29Z`; 현재 사람의 `wave 1 시작` 지시 범위
- 상태: `review-ready`

## 수행 내용

- 변경 목적: Node 24 + npm 기반의 framework 없는 strict TypeScript/Vite 프로젝트와 PR quality 최소 경로를 재현 가능하게 만든다.
- 변경 파일:
  - 런타임·패키지: `.nvmrc`, `package.json`, `package-lock.json`, `.gitignore`
  - 정적 품질: `tsconfig.json`, `eslint.config.js`, `.prettierrc.json`, `.prettierignore`
  - 최소 앱: `index.html`, `src/main.ts`, `src/main.test.ts`, `vite.config.ts`
  - production smoke: `playwright.config.ts`, `tests/e2e/scaffold.spec.ts`
  - PR workflow: `.github/workflows/quality.yml`
  - 조정 기록: `docs/coordination/tasks/SG-005.yaml`, 이 handoff
- 의도적으로 변경하지 않은 범위: 기존 제품/결정/계약/QA/STATUS 문서, `spikes/**`, 게임 규칙, 완성 UI와 CSS, domain/game/adapters, Pages artifact·deploy, 공개 URL.
- 임시 소유권: 현재 `index.html`과 `src/main.ts`의 제목/boot 상태는 scaffold marker일 뿐이며 SG-006이 접근 가능한 DOM shell로 교체한다. SG-008은 test fixture/config를 확장하고 SG-009는 Vite/quality의 private artifact 경로를 확장한다.

## 수용 기준 증거

| AC | 증거(테스트 이름, 스크린샷, 로그 또는 수동 절차) | 결과 |
|---|---|---|
| `SG-005-AC01` / `AC-R04` / `CHK-R04` | `npm ls --depth=0`: Phaser 4.2.1, Vite 8.1.5, TypeScript 6.0.3, ESLint 10.7.0 flat config, Prettier 3.9.5, Vitest 4.1.10, Playwright 1.61.1. `tsconfig.json`은 strict와 추가 안전 옵션을 활성화한다. | PASS |
| `SG-005-AC02` / `ENV-PR-STATIC` | Node `v24.14.0`, npm `11.18.0` 환경에서 clean lock install과 `format:check`, `lint`, `typecheck`, `test`, `build`, `verify`가 모두 exit 0. | PASS |
| `SG-005-AC03` / `ENV-PR-C1366` | Vitest `application entry` 1개가 실제 `src/main.ts`를 import해 PASS. Playwright `boots the production entry without browser or asset failures`가 Chromium 1366×768에서 기본 base와 `/snake-game/` base 각각 1/1 PASS; console error, pageerror, request failure, HTTP 4xx/5xx 0, 모든 local response가 base prefix 안에 있음. | PASS |
| `SG-005-AC04` | `quality.yml` YAML parse PASS, `permissions: contents: read`, checkout credential 비보존, 공식 Actions v7 full-SHA pin, `GITHUB_REPOSITORY`에서 검증·정규화된 `/<repo>/` 산출, 동일 env의 build/preview smoke. Pages API/upload/deploy/write/id-token/environment와 `pull_request_target` 검색 결과 0건. | PASS |
| `SG-005-AC05` / `NFR-SC04` / `NFR-M03` | lockfile은 아래 직접 의존성 이유로 한 번 생성했고 `npm ci` PASS. `git ls-files dist coverage playwright-report test-results node_modules` 출력 0줄. `.gitignore`가 생성물과 브라우저 로컬 경로를 차단한다. | PASS |

## 실행한 검증

| 명령/환경 | exit code | 실제 결과 |
|---|---:|---|
| bundled Node `node --version`; temporary npm CLI `npm --version` | 0 | `v24.14.0`; `11.18.0` |
| `PLAYWRIGHT_SKIP_BROWSER_DOWNLOAD=1 npm ci` (browser는 별도 고정 Playwright CLI로 설치) | 0 | lockfile v3 clean install; npm debug 종료 `info ok` |
| `npm run format:check` | 0 | 모든 scoped HTML/JS/JSON/TS/YAML 및 미래 `src/tests` CSS glob PASS |
| `npm run lint` | 0 | ESLint `--max-warnings 0` PASS |
| `npm run typecheck` | 0 | strict TypeScript PASS |
| `npm run test` | 0 | Vitest 1 file / 1 test PASS; Playwright와 보존된 spike는 명시적으로 unit 수집 제외 |
| `npm run build` | 0 | Vite 8.1.5; JS 1.05 kB(gzip 0.55 kB), HTML 0.48 kB(gzip 0.30 kB) |
| `npm run test:e2e` / Chromium 149.0.7827.55, 1366×768 | 0 | production root-base smoke 1/1 PASS |
| `npm run verify` / 동일 Node·Chromium | 0 | format → lint → typecheck → unit → build → production smoke 전체 PASS, Playwright 1/1 |
| `npm run build -- --base /snake-game/` | 0 | 하위 경로 artifact build PASS; HTML 0.49 kB, JS 1.05 kB |
| `PLAYWRIGHT_BASE_PATH=/snake-game/ PLAYWRIGHT_USE_EXISTING_BUILD=1 npm run test:e2e` | 0 | build 재실행 없이 `/snake-game/` document/hashed JS 200, base prefix 보존, Chromium 1/1 PASS |
| `git diff --check db5711e..15f6b5a` | 0 | whitespace 오류 0 |
| `git diff --name-only db5711e..15f6b5a` | 0 | packet의 구현 허용 경로 16개만 출력 |
| `git ls-files dist coverage playwright-report test-results node_modules` | 0 | 출력 0줄 |
| Ruby YAML parse(packet + workflow) | 0 | `YAML parse: PASS` |
| workflow full-SHA 정규식 / 금지 Pages·deploy 권한 정규식 | 0 / 1(expected) | 40자리 공식 Action pin 2개; 금지 패턴 match 0 |

실행하지 못한 검증과 이유:

- 실제 GitHub-hosted PR job: 원격 push/PR 생성 권한을 이번 지시에서 확대하지 않았으므로 실행하지 않았다. 같은 명령과 repo-base production smoke는 로컬에서 재현했다.
- 공식 Claude Sonnet 5 독립 리뷰: 현재 Codex 실행 환경에서 다른 플랫폼 runtime을 가장할 수 없다. 이 리뷰 전에는 `verified` 또는 병합으로 올리지 않는다.
- sandbox 내부 첫 E2E 시도는 localhost bind가 `EPERM`으로 차단됐다. 같은 SHA/명령을 localhost 권한 환경에서 즉시 재실행해 기본 base와 repository base 모두 PASS했다.

## 결정과 위험

- 새 의존성:
  - `phaser@4.2.1`: D-001에서 고정한 유일한 게임 runtime.
  - `vite@8.1.5`, `typescript@6.0.3`, `@types/node@24.13.3`: Vite 8.1/Node 24 strict TS build. TypeScript 7은 `typescript-eslint@8.64.0`의 공식 `<6.1.0` peer 범위 밖이라 최신 호환 안정판 6.0.3을 선택했다.
  - `eslint@10.7.0`, `@eslint/js@10.0.1`, `typescript-eslint@8.64.0`, `globals@17.7.0`: 공식 flat config와 typed lint.
  - `prettier@3.9.5`: 일관된 format check.
  - `vitest@4.1.10`: 실제 entry import 단위 smoke.
  - `@playwright/test@1.61.1`: production Chromium/base-path smoke. 브라우저 binary는 Git에 포함하지 않는다.
- 새 결정 또는 기존 결정과의 차이: 없음. package version은 2026-07-18 공식 npm metadata의 Node 24/Vite 8.1 peer 범위 안에서 exact pin했다. `.nvmrc`는 승인 계약대로 Node 24 major를 고정한다.
- 알려진 위험/제약:
  - 현재 로컬 Node는 bundled `24.14.0`이고 `.nvmrc`는 최신 24.x LTS를 선택한다. 계약이 major 고정이므로 허용되지만 리뷰어는 자기 Node 24 patch를 기록해야 한다.
  - 도구 환경이 `NO_COLOR`와 `FORCE_COLOR`를 동시에 설정해 Playwright 자식 프로세스가 색상 환경 경고를 출력했다. console/page failure 수집은 0이며 앱/runtime 요청과 무관하다.
  - Actions checkout/setup-node v7은 GitHub-hosted runner의 현행 버전을 전제로 한다. 두 action은 node24 runtime이고 full commit SHA로 고정했다.
  - `package-lock.json`의 `@opentelemetry/api` 문자열은 Vitest의 설치되지 않은 optional peer metadata이며 제품 runtime 요청·분석 SDK가 아니다.
- 남은 작업과 권장 담당자: Claude Sonnet 5 medium이 이 SHA를 독립 리뷰한다. 승인 뒤 Codex integrator가 병합·STATUS 동기화하고 SG-006/007/008/009 packet을 선행 관계에 맞춰 준비한다.

## 리뷰어 재현 절차

1. `git switch agent/codex/SG-005-scaffold` 후 `git rev-parse HEAD`와 packet의 `head_sha` 구현 커밋을 확인하고, 변경 경로가 packet allowlist 안인지 비교한다.
2. Node 24에서 `npm ci` 후 `npm run verify`를 실행한다. Chromium이 없으면 `npm exec -- playwright install chromium`을 먼저 실행하되 binary를 커밋하지 않는다.
3. `npm run build -- --base /snake-game/` 후 `PLAYWRIGHT_BASE_PATH=/snake-game/ PLAYWRIGHT_USE_EXISTING_BUILD=1 npm run test:e2e`를 실행하고 1/1 PASS, HTTP/console/request failure 0을 확인한다.
