# 작업 인계: `SG-022` — M1 Phaser Canvas fallback

## 식별 정보

- 담당 AI / 모델 / 설정: Codex / Codex GPT-5 runtime / high
- 명세 버전 / task packet revision: `1.0-plan` / 1
- 기준 SHA(`base_sha`): `611b88c0f7c1816424c161ada8e5cc9b162e438e`
- 브랜치 / 구현 결과 SHA(`implementation_head_sha`):
  `agent/codex/SG-022-webgl-fallback` /
  `49865f66cc20077008cc5aaa310369220c14e8ee`
- 연결 Issue / PR: 없음 — 승인된 offline packet
- 승인자 / 승인 시각: human-user / `2026-07-23T18:09:40Z`
- 상태: `review-ready` (지정 Claude 리뷰 대기)

## 수행 내용

- 변경 목적: SG-021 M1의 강제 `Phaser.WEBGL` 설정을 `Phaser.AUTO`로 바꿔 WebGL이
  불가할 때 Phaser가 Canvas 렌더러를 선택할 수 있게 했다. **근본 원인:** 강제 WebGL은
  초기화 실패 시 GameScene update loop까지 만들지 못해 논리 tick도 중단시킨다.
- 변경 파일:
  - `src/game/bootstrap.ts`: renderer type 식별자 하나를 `WEBGL`에서 `AUTO`로 변경.
  - `src/game/bootstrap.test.ts`: Phaser config을 모킹해 AUTO 선택을 검증하는 회귀 테스트 추가.
  - `docs/coordination/tasks/SG-022.yaml`: 승인·claim·범위·검증 기록.
  - `docs/coordination/handoffs/SG-022.md`: 이 인계 기록.
- 의도적으로 변경하지 않은 범위: SG-021 L1~L5, `tests/**`, domain/UI/adapter/config,
  package/lockfile, build·Playwright 설정, `STATUS.md`, 계약·계획·결정 문서. 새 의존성 없음.

## 수용 기준 증거

| AC | 증거(테스트 이름, 스크린샷, 로그 또는 수동 절차) | 결과 |
|---|---|---|
| SG-022-M1-AC01 | `mountPhaserGame`의 모킹된 `Phaser.Game` config을 검사하는 `src/game/bootstrap.test.ts` | PASS — `type: 'auto-renderer'` |
| SG-022-M1-AC02 | 수정 전 같은 테스트는 `received type: 'webgl-renderer'`로 exit 1; 수정 후 exit 0 | PASS |
| 브라우저 회귀 보강 | 깨끗한 `npm run test:e2e` 재실행 | PASS — Chromium 16/16 |

## 실행한 검증

| 명령/환경 | exit code | 실제 결과 |
|---|---:|---|
| `npm ci` (Node 22.18.0 / npm 10.9.3) | 0 | 157 packages 설치. package가 Node 24를 요구한다는 `EBADENGINE` 경고만 있음. |
| `npm run test -- src/game/bootstrap.test.ts` (수정 전) | 1 | `expected auto-renderer`, 실제 `webgl-renderer`; 결함 재현. |
| `npm run test -- src/game/bootstrap.test.ts` (수정 후) | 0 | 1/1 PASS. |
| `npm run format:check` | 0 | Prettier 일치. |
| `npm run lint` | 0 | 오류·경고 0. |
| `npm run typecheck` | 0 | 오류 0. |
| `npm run test` | 0 | 15 files / 133 tests PASS. |
| `npm run build` | 0 | build 성공; 기존 Phaser bootstrap chunk >500kB 경고만 있음. |
| `git diff --check` | 0 | 공백 오류 0. |
| `npm run test:e2e` (첫 시도) | 1 | sandbox가 preview port bind를 막음 (`EPERM 127.0.0.1:4173`). |
| `npm run test:e2e` (권한 허용 후 첫 시도) | 1 | 이미 실행 중이던 preview server에 붙었다가 종료되어 포트/연결 실패. |
| `npm run test:e2e` (깨끗한 재실행) | 0 | Chromium 16/16 PASS (2.4m). |

실행하지 못한 검증과 이유: 없음. 첫 두 E2E 실패는 환경/서버 충돌로 분리했고, 서버 종료 후
동일 명령의 clean rerun으로 통과를 확인했다.

## 결정과 위험

- 새 의존성: 없음.
- 새 결정 또는 기존 결정과의 차이: 없음. Phaser 4.2.1(D-001)은 유지하며 renderer fallback만
  활성화한다.
- 알려진 위험/제약: 회귀 테스트는 config 전달을 결정론적으로 검증한다. 이 환경에서 WebGL을
  의도적으로 차단한 실브라우저 Canvas fallback 시나리오는 별도 QA 범위다.
- 남은 작업과 권장 담당자: 지정 리뷰어 Claude가 M1의 최소 범위와 config-level 증거를 검토.
  SG-021의 L1~L5 및 SG-020 결함은 이 packet 범위 밖이므로 별도 packet/PR로 유지한다.

## 리뷰어 재현 절차

1. `git switch agent/codex/SG-022-webgl-fallback` 후 `npm ci` (프로젝트 기준 Node 24 사용).
2. `npm run test -- src/game/bootstrap.test.ts`로 AUTO config assertion을 확인한다.
3. `npm run format:check && npm run lint && npm run typecheck && npm run test && npm run build`를 실행한다.
4. 로컬 preview 실행을 허용할 수 있는 환경에서 `npm run test:e2e`를 실행한다.
