# 작업 인계: `SG-010`

## 식별 정보

- 담당 AI / 모델 / 설정: Codex / 현재 Codex GPT-5 기반 runtime / 설정 label 미노출.
  계획상 기준은 Codex 5.6 Sol / very-high이나 이 세션은 정확한 alias나 동등 설정을 확인할
  수 없어 동등성을 주장하지 않는다.
- 명세 버전 / task packet revision: `1.0-plan` / revision 1
- 기준 SHA(`base_sha`): `a9267a79cc755d049838d158cca31bad87f71528`
- 브랜치 / 구현 SHA(`head_sha`): `agent/codex/SG-010-simulation` /
  `03f99aa0d7e9d8864fcba513608292f262f3c7e8`
- 작업 원본: `docs/coordination/tasks/SG-010.yaml` 단일 offline packet. packet 작성 직전
  GitHub의 열린 Issue/PR은 각각 0건이었고, 사람은 원격보다 앞선 local main SHA를 기준으로
  명시 승인했다.
- 승인자 / 승인 시각: Codex integrator / `2026-07-19T08:31:05Z`. 사람의 직접 지시
  "Codex integrator가 로컬 main a9267a79cc755d049838d158cca31bad87f71528 기준 SG-010
  revision 1 offline packet을 작성·승인하고, 현재 런타임 편차를 기록한 뒤 진행하라"에
  근거한다.
- 선행 작업: H0b, accepted SG-003 revision 2 계약, merged SG-007 모두 충족.
- 연결 Issue / PR: 없음. offline source만 사용했으며 원격 push·PR·배포·merge는 수행하지 않았다.
- 상태: `review-ready`. 지정 독립 리뷰어 Claude Opus 4.8 / high의 검증 전이므로 `verified`나
  `merged`를 주장하지 않는다.

## 수행 내용

- 변경 목적: Phaser/DOM/시간과 무관한 순수 TypeScript로 READY/PLAYING 방향 enqueue와 한
  논리 tick 전체를 결정론적으로 구현한다. 마지막 승인 예약 방향, 2칸 queue, 벽/몸 충돌,
  비성장 tail 예외, 성장·점수·속도, row-major free-cell 음식, 만석 승리, 단일 종료 event를
  accepted 계약 순서대로 처리한다.
- 변경 파일:
  - `src/domain/direction.ts`: 방향 반대 관계와 integer-cell 이동 helper.
  - `src/domain/snake-simulation.ts`: `enqueueDirection`, `step`, 1~10 고정 tick 순서,
    충돌/점수/속도/승리 전이.
  - `src/domain/snake-simulation.test.ts`: queue·tick 순서·tail·충돌·성장·속도 하한·만석·
    frozen input·100틱 동일 seed 재현 29 tests.
  - `src/domain/food-spawner.ts`: SG-007 리뷰의 도달 불가 `?? null`을 explicit invariant
    failure로 교체.
  - `src/domain/index.ts`: accepted 공개 함수 `enqueueDirection`, `step` export.
  - `docs/coordination/tasks/SG-010.yaml`, 이 handoff: 승인·claim·범위·검증 증거.
- 의도적으로 변경하지 않은 범위:
  - 공개 타입·command/event와 `GAME_CONFIG`, accepted 계약/결정/QA/상태 문서를 바꾸지 않았다.
  - scheduler accumulator/FPS 차감, pause router, Phaser/DOM/UI, high-score adapter, listener
    lifecycle는 각각 후속 SG-012/013/014/016 범위라 구현하지 않았다.
  - `tests/**`, root 설정, package/lockfile, workflow, UI mirror를 건드리지 않았다. 단위 테스트는
    task allowlist인 `src/domain/**`에 colocate했다.
  - `dist`, `node_modules`, coverage, Playwright 결과물을 Git에 추가하지 않았다.

## 수용 기준 증거

| AC | 증거(테스트 이름 또는 코드 구조) | 결과 |
|---|---|---|
| `SG-010-AC01` / `AC-G01~G03` | `READY accepts ... without moving`, `READY rejects Left`, `PLAYING accepts right-up-left`, `rejects ... last reservation before ... capacity`, `ignores a third ... reservation`가 READY 예외와 검증 순서·2칸 상한을 확인한다. | PASS |
| `SG-010-AC02` / domain `AC-G02,G04` | `1 consumes at most one direction and 2 moves exactly one cell`; `step` signature에는 delta/FPS/time이 없고 frozen input은 유지된다. scheduler의 stepDuration 차감은 SG-012 범위다. | PASS |
| `SG-010-AC03` / `AC-G06,G07` | `3 ends on a wall ... once`, `4-5 allows ... tail`, `5 rejects a non-tail body collision`이 마지막 정상 head, 실제 attemptedCell, queue clear, terminal 재호출 event 0을 확인한다. 성장 시 전체 snake를 충돌 대상으로 쓰되 accepted DN01에 따라 invalid 성장-tail fixture는 만들지 않았다. | PASS |
| `SG-010-AC04` / domain `AC-G05,G08` | `6-9 grows once, scores once, accelerates on the fifth food`와 `8 caps slow/normal speed` 4 cases가 단일 성장/점수/event와 두 난이도 하한을 검증한다. high-score 저장과 scheduler 차감은 범위 밖이다. | PASS |
| `SG-010-AC05` / `AC-G05,G09` | 기존 `maps free-cell index ... without retrying`, 신규 성장 spawn의 `nextInt(396)` 1회, `9 wins a full board without RNG and orders foodEaten before gameWon`이 retry 없음·비중첩·만석 event 순서를 확인한다. | PASS |
| `SG-010-AC06` / domain `AC-G10` | SG-007 reset tests가 queue/점수/길이/속도/food를 초기화하고 전체 suite에 계속 통과한다. SG-010은 listener/accumulator 상태를 추가하지 않았다. | PASS |
| `SG-010-AC07` / 결정론 | `does not mutate frozen enqueue or step inputs`와 `produces identical dispositions, snapshots, events, and RNG calls for 100 ticks`가 두 독립 seeded source의 100 actual playing ticks를 value 비교한다. | PASS |
| `SG-010-AC08` / `D-003` | production `src/domain` 금지 import/global scan 일치 0, strict typecheck/build PASS, package/lockfile diff 0. | PASS |

## 실행한 검증

최종 증거는 구현 SHA `03f99aa0d7e9d8864fcba513608292f262f3c7e8`, bundled Node
`v24.14.0`, npm CLI `10.9.3`, 기존 lockfile에서 수집했다.

| 명령/환경 | exit code | 실제 결과 |
|---|---:|---|
| Node 24로 `npm ci` | 0 | 고정 패키지 139개 clean install, package/lockfile 변경 0. |
| `npm run format:check` | 0 | 모든 대상 Prettier PASS. |
| `npm run lint` | 0 | ESLint `--max-warnings 0` PASS. |
| `npm run typecheck` | 0 | strict TypeScript PASS. |
| `npm run test` | 0 | Vitest 4 files, 69 tests PASS. SG-010 신규 29 tests 포함. |
| `npm run build` | 0 | Vite 8.1.5 production build PASS, 8 modules. |
| `npm run test:e2e` / Chromium 1366×768 | 0 | production preview smoke 1/1 PASS; page/asset/browser failure 없음. |
| packet forbidden import/global `rg` 부정 검사 | 0 | Phaser, DOM/window, timers/RAF, storage, Web Audio, Date/performance, `Math.random`, crypto/globalThis 일치 0. |
| `git diff --check base_sha..head_sha` | 0 | whitespace 오류 0. |
| `git diff --name-status base_sha..head_sha` | 0 | packet 1개와 `src/domain/**` 5개만 변경되어 allowlist와 일치. |
| `git diff -- package.json package-lock.json src/config ... tests` | 0 | forbidden 경로 diff 출력 0. |
| `git ls-files dist coverage playwright-report test-results node_modules` | 0 | 추적 생성물 0줄. |
| Ruby stdlib packet YAML parse/핵심 필드 검사 | 0 | id/revision/status/base/approval/claim parse PASS. |

사전 실행에서 확인하고 해소한 환경/형식 실패:

- 최초 format/lint/typecheck/test는 `node_modules`가 없어 각각 exit 127/127/2/127이었다. 기본
  셸 Node도 `v22.18.0`이었다. bundled Node `v24.14.0`으로 고정해 `npm ci`부터 다시 실행했다.
- 첫 유효 format은 신규 2파일에서 exit 1, lint는 test helper 미사용 인자와 matcher `any`로
  exit 1이었다. 의미 변경 없이 포맷하고 assertion을 명시화한 뒤 최종 검사를 모두 통과했다.
- sandbox 안 첫 `test:e2e`는 localhost `127.0.0.1:4173` bind `EPERM`으로 exit 1이었다.
  동일 구현 SHA·Node 24·명령을 localhost 허용 환경에서 재실행해 Chromium 1/1 PASS했다.
- Playwright child process의 기존 `NO_COLOR`/`FORCE_COLOR` 동시 설정 경고는 남지만 앱
  console/page/request 실패와 무관하며 숨기거나 우회하지 않았다.

## 결정과 위험

- 새 의존성: 없음. `package.json`과 `package-lock.json` 변경 없음.
- 새 결정 또는 기존 결정과의 차이: 없음. SG-003 revision 2와 D-003을 그대로 구현했다.
- 구현 선택:
  - enqueue 반대 방향 판정은 queue 용량보다 먼저 수행하고 비교 기준을 마지막 승인 예약 방향으로
    고정했다. READY Right만 PLAYING 중복 규칙의 예외다.
  - tick 구조에 계약 번호 1~10을 보존해 wall→growth→self→move→score→food/won 순서를 코드와
    테스트 이름에서 읽을 수 있다.
  - food 선택의 검증된 index가 사라지는 불가능한 경우는 `null`(full-board 의미)로 축소하지 않고
    invariant error로 중단한다. RandomSource 범위 위반도 기존대로 보정하지 않는다.
- 알려진 위험/제약:
  - AC-G04의 렌더 FPS/가속 stepDuration 차감, AC-G08의 high score, AC-G10의 accumulator/listener는
    packet에 명시한 대로 domain 소유가 아니며 이 PASS가 그 후속 통합 증거를 대신하지 않는다.
  - SG-004-DN01 accepted 결정에 따라 음식 비중첩 상태에서 도달 불가능한 성장-동일-tail invalid
    fixture를 만들지 않았다. 구현 자체는 성장 여부를 먼저 고정하고 성장 시 현재 snake 전체를
    collision body로 사용한다.
  - `src/ui/contracts.ts` mirror는 이 packet의 금지 경로라 남아 있으며 UI/통합 packet에서 실제
    domain import로 교체해야 한다.
- 남은 작업과 권장 담당자: 지정 Claude 독립 리뷰 후 Codex integrator가 verified/merge를 판단한다.
  후속 SG-011은 독립 domain tests/coverage, SG-012는 scheduler/Phaser adapter를 맡는다.

## 리뷰어 재현 절차

1. 브랜치 `agent/codex/SG-010-simulation`의 구현 SHA
   `03f99aa0d7e9d8864fcba513608292f262f3c7e8`을 checkout한다.
2. Node 24에서 `npm ci` 후 packet의 여섯 npm 명령을 실행한다. localhost 제한 환경의
   Playwright만 같은 SHA로 bind 허용 환경에서 실행한다.
3. `git diff --name-status a9267a79cc755d049838d158cca31bad87f71528..03f99aa0d7e9d8864fcba513608292f262f3c7e8`가
   SG-010 packet과 `src/domain/**`에만 있는지 확인하고 package/lockfile/accepted 계약 diff 0을
   확인한다.
4. `CONTRACTS.md` §4~§6·§8과 `enqueueDirection`/`step` 및 29 tests를 대조한다. 특히 READY
   Right, full queue의 reversal 우선, one-direction consumption, wall-before-growth, tail slice,
   event payload/order, 5번째 음식 속도, no-RNG won, terminal no-op을 확인한다.
5. forbidden import/global scan, frozen-input test, 동일 seed 100틱 재현을 재실행하고 공개 type이나
   dependency 추가가 없는지 확인한다.
